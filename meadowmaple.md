# NovaLink 资源导航系统

NovaLink 是一个面向技术社区与内容创作者的轻量化外链资源导航与聚合平台。该项目定位为可自托管的书签管理与资源发布中间件，帮助开发者、运维人员与技术内容团队高效组织、分类和展示大量外部链接资源，同时提供友好的检索体验与可扩展的数据管理接口。

NovaLink 解决的核心问题在于，多数现有书签工具或导航站点缺乏对技术场景的精细化支持，例如批量链接导入、自定义分类权重、访问统计钩子以及基于标签的快速过滤。NovaLink 以静态生成结合动态接口的方式，兼顾性能与灵活性，适用于个人知识库、团队内部工具导航以及开源项目推荐聚合等场景。项目本身不存储任何第三方资源内容，仅提供链接元数据管理与展示层，因此天然规避内容版权风险，并易于与现有 CI/CD 工作流集成。

## 功能概览

- **多级分类与标签系统**：支持无限层级的分类目录以及扁平标签体系，每个链接可归属于多个分类和标签，便于从不同维度组织资源。

- **批量链接导入导出**：提供 CSV 与 JSON 格式的批量导入导出接口，支持从主流书签导出文件（如浏览器 HTML 书签）迁移数据，并自动识别标题、URL 和描述信息。

- **全文检索与过滤**：基于倒排索引实现轻量级全文搜索，支持按标题、描述、标签、分类等多条件组合过滤，响应时间控制在 200 毫秒以内。

- **可定制元数据字段**：允许管理员为每个链接自定义扩展字段（如适用场景、维护状态、访问频率建议），满足技术文档或项目推荐等垂直场景需求。

- **访问统计与热力分析**：内置点击计数与时间衰减算法，自动生成热门资源排行，并支持以 JSON 接口形式暴露统计结果供外部仪表盘使用。

- **静态化输出与动态回退**：支持全站静态 HTML 生成，适用于高访问量场景；同时保留动态 API 接口，用于实时数据更新和交互式管理操作。

## 应用场景

1. **技术团队内部知识库导航**：研发团队可将 NovaLink 部署于内网，集中管理常用的开发文档、API 参考、CI/CD 工具链入口和运维手册链接，通过分类和标签快速定位特定技术栈（如 Kubernetes、React、Go）的相关资源。

2. **开源项目推荐聚合站**：社区运营者可使用 NovaLink 维护精选开源项目列表，每个项目链接附带简介、技术标签和活跃度标记，访客可按领域或编程语言筛选，并查看项目热度趋势。

3. **个人学习路线资源整理**：技术学习者可借助 NovaLink 构建个人学习门户，按学习阶段（初级、中级、高级）和主题（算法、数据库、前端框架）组织视频教程、官方文档、练习平台等外部链接，形成体系化学习路径。

4. **活动或课程资料配套站点**：技术会议或在线课程的组织方可快速生成配套资源导航页，将演讲稿、示例代码仓库、延伸阅读材料统一收录，并在活动期间动态更新访问统计，了解资料受关注程度。

5. **多源资讯聚合展示**：内容编辑团队可将 NovaLink 作为中间层，聚合来自不同信息源的新闻、博客和发布公告链接，按时间线排列并标注来源标签，为读者提供一站式资讯入口。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，确保已安装 Git、Node.js（v18+）和 npm（v9+）。

```bash
# 1. 克隆项目仓库
git clone https://github.com/novalink-dev/novalink.git
cd novalink

# 2. 安装项目依赖
npm install

# 3. 配置环境变量（复制示例配置文件）
cp .env.example .env
# 编辑 .env 文件，至少设置 DATABASE_PATH 和 SITE_BASE_URL

# 4. 初始化数据库并导入示例数据
npm run db:init
npm run seed

# 5. 启动开发服务器（默认监听 3000 端口）
npm run dev
```

访问 `http://localhost:3000` 即可查看导航首页。如需构建生产环境静态文件，执行 `npm run build` 并输出 `dist` 目录内容至 Web 服务器。

## 安装要求

| 依赖 | 必需 | 说明 |
|------|------|------|
| Node.js 18.x 或更高版本 | 是 | 运行时与包管理基础环境，推荐使用 LTS 版本 |
| npm 9.x 或更高版本 | 是 | 依赖安装与脚本执行工具，随 Node.js 自动安装 |
| SQLite 3 | 是 | 内置数据库引擎，用于存储链接元数据与分类信息，无需额外安装 |
| 可用端口 3000（开发）/ 80 或 443（生产） | 是 | Web 服务监听端口，可按需通过环境变量修改 |
| Git 2.25+ | 推荐 | 用于克隆仓库和版本管理，非强制但便于更新 |
| 磁盘剩余空间 ≥ 200 MB | 是 | 存放代码、数据库文件及静态生成输出 |
| 内存 ≥ 512 MB | 推荐 | 保证构建过程和并发请求下的稳定运行 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户指南 | `/docs/user-guide/` | 如何添加链接、管理分类、使用搜索和过滤功能 |
| 管理员手册 | `/docs/admin/` | 如何进行批量导入导出、配置元数据字段、查看统计信息 |
| 开发文档 | `/docs/developer/` | 如何扩展数据模型、自定义前端主题、接入外部 API |
| 部署参考 | `/docs/deployment/` | 如何部署到 Linux 服务器、容器化方案、静态化与反向代理配置 |
| 常见问题 | `/docs/faq/` | 涵盖数据迁移、性能调优、权限管理等常见疑问 |

## 资源列表

以下为 NovaLink 收录的外部资源链接，按类别组织展示。所有链接均保持用户提供的原始格式。

资讯与新闻类

- <code>zhongwenzimugaoguingshipinw.org.cn</code>
- <code>gaoqingzhongwenzimudianshijuw.org.cn</code>
- <code>zaixiangaoqingzhongwenzimuw.org.cn</code>

影音与视频类

- <code>zaixianguankanrihandianshijuw.org.cn</code>
- <code>zhongwenzimuyingshigaoqingw.org.cn</code>
- <code>zaixianbofangzhongwenzimuw.org.cn</code>
- <code>gaoqingyingshimianfeiguankanw.org.cn</code>

在线平台与门户

- <code>mianfeiguankangaoqingdianyingw.org.cn</code>
- <code>zaixianshipinbofangpingtaiw.org.cn</code>
- <code>zaixianguankanmianfeiduanjuw.org.cn</code>

## 项目结构

```
novalink/
├── src/                           # 核心源代码目录
│   ├── api/                       # 后端 API 路由与控制器
│   │   ├── v1/                    # API 版本 1 实现
│   │   │   ├── links.js           # 链接增删改查接口
│   │   │   ├── categories.js      # 分类管理接口
│   │   │   └── stats.js           # 访问统计接口
│   │   └── middleware/            # 请求中间件（日志、鉴权、限流）
│   ├── core/                      # 核心业务逻辑层
│   │   ├── search.js              # 全文检索引擎实现
│   │   ├── importer.js            # 批量导入处理器（CSV/JSON/HTML）
│   │   └── exporter.js            # 数据导出器
│   ├── db/                        # 数据库相关
│   │   ├── migrations/            # 数据库版本迁移脚本
│   │   ├── models/                # 数据模型定义（链接、分类、标签）
│   │   └── seed/                  # 初始示例数据填充脚本
│   ├── frontend/                  # 前端资源
│   │   ├── components/            # Vue/React 可复用组件（视具体实现）
│   │   ├── pages/                 # 页面级组件（首页、分类页、详情页）
│   │   └── static/                # 静态资源（CSS、图片、字体）
│   ├── generator/                 # 静态站点生成器
│   │   ├── renderer.js            # HTML 模板渲染引擎适配
│   │   └── watcher.js             # 文件变动监听与增量生成
│   └── utils/                     # 通用工具函数
│       ├── validator.js           # URL 与输入校验
│       └── cache.js               # 内存缓存与过期策略
├── config/                        # 配置文件目录
│   ├── default.json               # 默认配置（端口、分页大小、缓存时间）
│   ├── production.json            # 生产环境覆盖配置
│   └── development.json           # 开发环境覆盖配置
├── tests/                         # 单元测试与集成测试
│   ├── unit/                      # 单测用例（核心函数与工具）
│   └── integration/               # 接口与数据库集成测试
├── docs/                          # 文档目录（用户、管理、开发、部署）
│   ├── user-guide/
│   ├── admin/
│   ├── developer/
│   └── deployment/
├── scripts/                       # 辅助脚本（部署、备份、数据迁移）
│   ├── deploy.sh                  # 一键部署脚本（生产环境）
│   └── backup.sh                  # 数据库定时备份脚本
├── .env.example                   # 环境变量配置模板
├── package.json                   # npm 项目配置与依赖列表
├── README.md                      # 项目介绍文档（本文件）
└── LICENSE                        # MIT 许可证文本
```

## 贡献指南

1. 在 GitHub 上 Fork 本项目仓库，并克隆到本地开发环境。建议在 `develop` 分支基础上新建功能分支，命名格式为 `feature/简要描述` 或 `fix/问题编号`。

2. 安装依赖并启动开发服务器后，编写或修改代码。请确保新增功能包含对应的单元测试，测试覆盖率不低于 80%。提交前执行 `npm run lint` 和 `npm test` 以通过代码风格检查和全部测试用例。

3. 提交信息请遵循 Conventional Commits 规范（如 `feat: 添加标签过滤接口`、`fix: 修复搜索分页偏移错误`），并附上清晰的问题描述或关联 Issue 编号。

4. 发起 Pull Request 至主仓库的 `develop` 分支，并在 PR 描述中说明改动目的、实现方式和可能的影响范围。维护者将在 3 个工作日内进行代码审阅。

5. 若涉及文档更新（如新增配置项或 API 变更），请同步修改 `/docs` 下对应文档，确保用户手册与代码行为一致。

## 常见问题

**Q：如何从其他书签工具（如 Raindrop.io 或浏览器导出的 HTML）迁移数据到 NovaLink？**

A：NovaLink 的导入模块支持解析标准的 Netscape 书签 HTML 格式，以及自定义的 CSV 模板。您可在管理后台的“导入”页面选择文件并上传，系统会自动识别格式并映射字段。对于大批量数据（超过 1000 条），建议使用命令行脚本 `npm run import -- --file=bookmarks.html` 以规避超时限制。

**Q：访问统计的热力排名如何计算？是否会频繁刷新导致性能问题？**

A：系统采用基于时间衰减的威尔逊置信区间算法，综合点击总数、最近 7 天点击量和链接发布时间计算得分。统计更新默认采用异步队列方式，每 5 分钟批量写入数据库，且前端展示的排名结果缓存 10 分钟，因此高并发下不会产生显著的数据库压力。

**Q：NovaLink 是否支持多用户或权限管理？**

A：当前版本（v1.x）为单用户管理模式，即所有访问者均可查看资源，但管理操作（增删改）仅限本地配置的管理员账户。从 v2.0 规划开始，将增加基于角色的访问控制（RBAC），支持多编辑者与审阅者角色。如需多用户协作，目前可通过部署多个实例并共享同一数据库的方式临时实现。

## 许可证

MIT License。允许免费使用、修改、分发和再授权，仅需保留原始版权声明和许可声明。详细条款请查阅项目根目录下的 LICENSE 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:36
