# HrefHub 开源导航站

HrefHub 是一个面向技术社区与内容创作者的轻量级外链资源导航系统。项目定位于为中小型技术团队、独立开发者及内容运营者提供一套可自部署、可扩展的优质资源收录与管理工具。HrefHub 不生产内容，而是通过结构化的外链组织方式，帮助用户高效整理、展示和分享有价值的网络资源，解决信息分散、链接失效、分类混乱等常见问题。

HrefHub 的核心目标用户包括开源文档维护者、技术教程作者、社区运营人员以及任何需要对外输出资源列表的长期主义者。通过 HrefHub，用户可以快速建立属于自己的资源导航页，并利用标准化模板持续迭代内容，无需重复编写繁琐的页面结构。

## 功能概览

- **一键导入外链清单**：支持批量导入用户提供的原始 URL 列表，自动去重并生成初始导航条目。
- **智能分类与标签系统**：允许用户为每个链接分配类别、标签及简要说明，便于后续筛选与检索。
- **链接健康状态检测**：内置定时任务，定期对收录的 URL 发起可用性探测，自动标记异常链接。
- **项目快照导出**：支持将整个导航站导出为静态 Markdown 文档或 JSON 数据包，便于备份与跨平台迁移。
- **可定制化展示模板**：提供多套列表与卡片布局模板，用户可根据场景调整页面密度与视觉风格。
- **全文检索与过滤**：基于标题、描述、标签及域名关键词，提供毫秒级本地检索能力。
- **访问统计与排行**：记录每个链接的点击频次，支持展示热门资源 Top N。
- **RSS 订阅输出**：为每个分类或标签生成独立的 RSS 订阅源，方便外部订阅者跟踪更新。

## 应用场景

- **技术团队内部知识库导航**：研发团队可使用 HrefHub 整理常用开发文档、API 参考、组件库及运维面板地址，统一团队入口，降低新成员上手成本。
- **开源项目附属资源页**：开源项目维护者可借助 HrefHub 为项目仓库配套一个“生态资源列表”，集中展示相关教程、插件、示例代码及社区站点，与 README 互补。
- **内容运营专题汇总**：内容创作者在撰写系列文章或报告时，可将文中提及的所有参考链接通过 HrefHub 汇总成一页，方便读者一站式查阅，同时提升自身内容的专业度与复用性。
- **个人书签管理与分享**：个人开发者可将常年积累的浏览器书签迁移至 HrefHub，并选择性地公开分享特定分类，形成个人品牌下的资源名片。
- **活动或课程配套资料站**：技术会议、在线课程或黑客松活动主办方，可使用 HrefHub 快速发布配套资料索引，活动结束后仍可长期维护，成为活动的延续价值。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，要求系统已安装 Git 与 Node.js 18.x 以上版本。

```bash
# 1. 克隆项目仓库
git clone https://github.com/hrefhub/hrefhub-starter.git
cd hrefhub-starter

# 2. 安装项目依赖
npm install

# 3. 启动开发服务器
npm run dev
```

启动成功后，访问本地 `http://localhost:3000` 即可进入 HrefHub 管理面板。首次启动将自动创建示例数据，您可通过管理界面的“导入链接”功能，直接将资源列表中的 URL 批量添加至系统。

## 安装要求

在部署 HrefHub 至生产环境前，请确保运行环境满足以下依赖条件。推荐使用 Docker 或 Node.js 原生部署方式。

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x LTS 或 20.x LTS | 运行时基础环境，推荐使用官方二进制包或 nvm 管理 |
| npm | 9.x 或 10.x | 包管理器，随 Node.js 一同安装 |
| SQLite | 3.x (内置) | 默认轻量级数据库，无需额外安装，适用于小型部署 |
| PostgreSQL | 14.x 或 15.x (可选) | 生产环境推荐替换 SQLite，需单独安装并配置连接字符串 |
| Redis | 7.x (可选) | 用于会话存储与缓存加速，高并发场景建议启用 |
| Git | 2.25+ | 用于克隆仓库及后续版本更新 |
| Nginx / Caddy | 任意稳定版 (可选) | 推荐作为反向代理，处理 TLS 终结与静态资源缓冲 |

## 文档导航

为帮助您快速定位所需信息，以下表格梳理了 HrefHub 文档体系的主要层面与对应目录。

| 层面 | 目录 / 文件 | 回答的问题 |
|------|-------------|------------|
| 入门指南 | `/docs/getting-started/` | 如何从零开始搭建 HrefHub？首次部署需要配置哪些核心参数？ |
| 数据管理 | `/docs/data-management/` | 如何批量导入、导出或迁移链接数据？数据格式规范是什么？ |
| 定制开发 | `/docs/customization/` | 如何修改页面主题、添加自定义字段或编写新模板？ |
| 运维监控 | `/docs/operations/` | 如何配置健康检查、日志收集、备份策略及性能调优？ |
| API 参考 | `/docs/api/` | HrefHub 提供了哪些 RESTful API 接口？如何通过 API 管理资源？ |
| 常见集成 | `/docs/integrations/` | 如何与 GitHub Pages、Notion、Obsidian 等第三方工具协同工作？ |

## 资源列表

本项目的核心数据来源为以下用户指定的原始资源链接。所有链接按类别分组呈现，且严格保留原始格式（协议、域名、路径、大小写均不做任何改动）。HrefHub 本身不保证这些外部站点的可用性与内容合规性，仅作为技术示例展示外链管理能力。

**重要说明**：以下每个 URL 均使用 `<code>` 标签包裹，且不附加任何 Markdown 链接语法，完全遵循用户提供的原始字符串。

### 综合分类

- <code>guochanyoudayouhuang.org.cn</code>
- <code>wuyerenqi.org.cn</code>
- <code>yazhouchengrenyiquerqu.org.cn</code>
- <code>oumeizhongchu.org.cn</code>
- <code>tiantianyue.org.cn</code>
- <code>yirenjiujiu.org.cn</code>
- <code>sihujingpin.org.cn</code>
- <code>guochanrihanoumei.org.cn</code>
- <code>rihanmadou.org.cn</code>
- <code>oumeihouru.org.cn</code>

以上列表共包含 10 个顶级域名资源，均归属于同一批次导入（第 12/455 批）。在 HrefHub 实际运行环境中，这些链接将被分配至默认未分类组，并等待管理员进一步标注标签与描述。

## 项目结构

HrefHub 采用模块化单体架构设计，核心代码组织如下。目录树中每一行均附带注释说明其职责，便于开发者快速理解代码布局。

```
hrefhub-starter/
├── app/                           # 应用核心目录
│   ├── api/                       # RESTful API 路由层 (Next.js App Router)
│   │   ├── links/                 # 链接资源增删改查接口
│   │   ├── categories/            # 分类管理接口
│   │   └── health/                # 系统健康检查接口
│   ├── dashboard/                 # 管理仪表盘页面 (服务端组件)
│   │   ├── page.tsx               # 总览面板，展示统计卡片与最近活动
│   │   └── layout.tsx             # 仪表盘共用布局，含侧边导航
│   └── public/                    # 公开导航页面 (客户端渲染)
│       ├── page.tsx               # 前台首页，展示所有分类与链接列表
│       └── [slug]/                # 动态分类详情页
├── components/                    # 复用 UI 组件库 (Shadcn/ui 风格)
│   ├── LinkCard/                  # 链接卡片组件 (含点击追踪)
│   ├── SearchBar/                 # 全局搜索框组件
│   └── StatusBadge/               # 链接健康状态徽标组件
├── lib/                           # 工具函数与共享逻辑
│   ├── db/                        # 数据库连接池与 ORM 映射 (Prisma)
│   ├── crawler/                   # 链接健康检测定时任务 (node-cron)
│   └── export/                    # 数据导出为 Markdown / JSON 工具
├── prisma/                        # 数据库 Schema 与迁移文件
│   ├── schema.prisma              # 数据模型定义 (Link, Category, Tag)
│   └── migrations/                # 按时间顺序的迁移脚本
├── public/                        # 静态资源 (图片、字体、favicon)
│   ├── icons/                     # 分类默认图标集
│   └── logos/                     # 品牌相关矢量图
├── tests/                         # 单元测试与集成测试 (Jest + Supertest)
│   ├── api/                       # API 路由测试用例
│   └── utils/                     # 工具函数单元测试
├── docs/                          # 项目文档 (本文档即由该目录生成)
│   ├── getting-started/           # 入门指南分章节
│   └── operations/                # 运维手册
├── .env.example                   # 环境变量模板 (含数据库连接、JWT 密钥)
├── docker-compose.yml             # 本地开发及生产容器编排 (含 Postgres + Redis)
├── Dockerfile                     # 多阶段构建镜像定义
├── next.config.js                 # Next.js 构建与运行时配置
├── package.json                   # 项目依赖清单与脚本定义
└── README.md                      # 项目主说明文档 (即当前文件)
```

## 贡献指南

我们欢迎任何形式的社区贡献，包括但不限于功能建议、文档改进、缺陷修复及新资源推荐。请遵循以下步骤参与 HrefHub 的开发与维护：

1. **查阅现有 Issue 与讨论**：访问 GitHub Issues 页面，确认您要反馈的问题或建议尚未被他人提出。若已有相关议题，请在其下补充您的使用场景或复现步骤。
2. **Fork 主仓库并创建特性分支**：将主仓库 Fork 至个人账号，并基于 `main` 分支创建新的分支，命名格式为 `feature/简短描述` 或 `fix/问题编号`。
3. **编写或修改代码，并确保测试通过**：在本地完成开发后，运行 `npm run test` 确保所有单元测试与集成测试均通过。若新增功能，请同步补充对应的测试用例。
4. **提交变更并签署开发者原产地证书 (DCO)**：提交信息请遵循 Conventional Commits 规范（如 `feat: add batch import progress bar`）。所有提交必须包含 Signed-off-by 行，表明您同意 DCO 协议。
5. **发起 Pull Request 至主仓库**：在 PR 描述中详细说明变更目的、影响范围及测试情况。PR 需要至少一位维护者进行 Code Review，通过后即合并进入主分支。

## 常见问题

**问：HrefHub 是否必须依赖 PostgreSQL 和 Redis？能否仅用 SQLite 运行？**

答：HrefHub 默认内置 SQLite 驱动，在低并发或个人使用场景下，无需安装任何额外数据库即可直接运行。但若您计划在生产环境面向多用户提供服务，我们强烈建议切换至 PostgreSQL 以保障数据一致性，并启用 Redis 提升会话管理效率。切换方法请参考 `/docs/operations/database-switch.md` 文档。

**问：如何批量更新已收录链接的健康状态？检测频率可否自定义？**

答：健康检测任务默认每 6 小时执行一次，检测全部处于“启用”状态的链接。您可以在 `lib/crawler/scheduler.js` 文件中调整 `cronSchedule` 变量值，支持标准 cron 表达式。此外，管理员也可在仪表盘的“链接管理”页面手动触发单条链接的即时检测，检测结果会在 10 秒内刷新显示。

**问：HrefHub 是否支持多用户权限管理？如何限制普通用户仅能查看不能编辑？**

答：当前版本（v1.0.x）支持基于角色的访问控制（RBAC），内置“管理员”与“访客”两种角色。管理员拥有全部增删改查权限，访客仅能浏览前台页面与检索链接，无法访问仪表盘。您可在首次启动时通过环境变量 `DEFAULT_USER_ROLE` 指定新建用户的默认角色，或通过管理后台手动调整已有用户角色。

## 许可证

HrefHub 采用 MIT 许可证进行分发。您可以在遵守许可证条款的前提下，自由使用、修改、复制、合并、发布、再许可及出售本软件的副本。完整许可证文本请参见项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
