# CloudLink 资源聚合导航系统

CloudLink 是一个专为技术社区、内容创作者及研究人员设计的高效外链资源聚合与导航系统。本项目定位于解决海量分散网络资源的管理、分类、检索与持续集成问题，目标用户包括开源项目维护者、技术文档编写者、在线教育平台运营方以及各类需要系统化整理外链资源的技术团队。通过结构化的元数据标注、自动化的链接可用性检测与友好的用户界面，CloudLink 能够显著降低资源管理成本，提升信息复用效率，并确保所有外链资源的长期可访问性与合规性。

## 功能概览

- **多层级资源分类体系**：支持按地域、行业、内容类型、语言等维度对链接进行动态标签化管理，便于构建复杂的导航树。
- **自动化健康检查**：内置定时任务，可对收录的所有外链进行 HTTP/HTTPS 状态码校验，自动标记失效链接并生成告警日志。
- **元数据增强引擎**：对每条链接自动抓取页面标题、描述、关键词及 favicon，丰富资源预览信息。
- **全文检索与过滤**：基于倒排索引实现标题、描述、标签及域名关键词的快速搜索，支持多条件组合筛选。
- **导入导出标准格式**：支持批量导入 URL 列表（CSV/JSON），并支持导出为 Markdown、HTML 或结构化 JSON 报告。
- **访问统计与热度分析**：记录每条外链的点击次数、最后访问时间及来源 IP 聚合（匿名化），生成热度排行。
- **用户权限与审核流**：支持多用户角色（管理员、编辑者、访客），提供链接提交、审核、发布的标准工作流。
- **RESTful API 全量接口**：所有核心功能均提供 JSON API，便于与第三方系统集成或进行二次开发。

## 应用场景

- **开源项目文档站外链管理**：当开源项目 README 或官方文档需要引用大量第三方依赖、教程、工具站时，使用 CloudLink 维护一个集中化的外链仓库，并通过 API 动态渲染，避免硬编码链接散落在各处导致维护困难。
- **技术社区资源聚合页**：技术论坛或问答社区可利用 CloudLink 创建“精选工具”、“学习路径”、“行业报告”等导航板块，由社区编辑共同维护，提升用户停留时长与信息获取效率。
- **在线教育课程参考资料库**：培训机构或 MOOC 平台可为每门课程配备独立的资源导航子站点，收录论文、数据集、代码仓库、视频地址等，并定期检查链接有效性，保障学员学习体验。
- **企业内部知识库外链治理**：企业内部 Wiki 中往往存在大量过期或权限变更的外部链接，通过 CloudLink 统一代理和审计，可降低安全风险，同时提供访问统计以辅助知识资产评价。
- **个人研究文献与数据源管理**：研究人员可自部署 CloudLink 作为个人研究基础设施，分类保存各类数据门户、标准文档、学术期刊地址，并利用全文检索快速定位特定主题的源站。

## 快速开始

以下步骤适用于 Linux / macOS / WSL2 环境，要求已安装 Git、Node.js 18+ 和 pnpm。

```bash
# 1. 克隆代码仓库
git clone https://github.com/cloudlink-io/cloudlink.git
cd cloudlink

# 2. 安装项目依赖（使用 pnpm 工作空间）
pnpm install --frozen-lockfile

# 3. 配置环境变量（复制示例配置并修改数据库连接等）
cp .env.example .env.local

# 4. 执行数据库迁移与种子数据初始化
pnpm run db:migrate
pnpm run db:seed

# 5. 以开发模式启动服务（前端 + API + 定时任务）
pnpm run dev
```

访问 `http://localhost:3000` 即可进入系统。默认管理员账号为 `admin@cloudlink.local`，密码为 `changeme-2024`，首次登录后请务必修改。

## 安装要求

生产环境部署前，请确保满足以下依赖条件。推荐使用 Docker Compose 一键部署，但若选择原生方式，请参考表格中的版本与配置建议。

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 运行时环境，需支持 ES2022 特性。推荐使用官方二进制或 nvm 管理。 |
| pnpm | 8.x 或 9.x | 包管理器，用于加速依赖安装与工作空间管理。禁止使用 npm 或 yarn。 |
| PostgreSQL | 14.x 或 15.x | 主数据库，需启用 pg_trgm 扩展以支持全文搜索。建议配置连接池大小 20。 |
| Redis | 7.x | 缓存与队列后端，用于会话存储、限流及定时任务锁。需开启持久化。 |
| MinIO / S3 兼容存储 | 最新稳定版 | 可选但推荐，用于存储链接快照、截图及导入文件。若不配置则使用本地文件系统。 |
| Nginx | 1.24+ | 反向代理与静态资源服务，生产环境必需，用于负载均衡与 TLS 终止。 |
| PM2 | 5.x | 进程守护工具，用于管理 API 服务、Worker 进程及调度器。 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `/docs/user-guide/` | 如何注册、登录、添加链接、创建分类、设置标签及查看统计数据？ |
| 管理员指南 | `/docs/admin-guide/` | 如何配置审核流、调整健康检查频率、管理用户权限及查看系统日志？ |
| API 参考 | `/docs/api-reference/` | 所有 RESTful 接口的请求/响应示例、错误码说明及速率限制策略是什么？ |
| 部署运维 | `/docs/deployment/` | 支持哪些部署方式（Docker/K8s/裸机）？如何配置 HTTPS、备份与监控告警？ |
| 开发者文档 | `/docs/developer/` | 项目架构、核心模块说明、如何编写插件、如何提交 PR 以及调试技巧？ |
| 变更日志 | `/CHANGELOG.md` | 每个版本的特性、修复与不兼容变更记录，以及升级注意事项。 |

## 资源列表

本导航系统初始收录的资源覆盖多个领域，所有链接均经过人工初步筛选与分类。下列地址为系统内置示例数据，用户可根据自身需求增删或修改。

**文化教育类**
- <code>shufuzipai.org.cn</code>
- <code>yazhouchuanmei.org.cn</code>
- <code>zhongwenzimuzhongchu.org.cn</code>

**综合福利与社区**
- <code>chunshuifuli.org.cn</code>
- <code>langrenzonghewang.org.cn</code>

**区域特色与专业内容**
- <code>oumeirihandiyiye.org.cn</code>
- <code>xiangjiaojiujiujingpinririzaoyeyezao.org.cn</code>
- <code>zhongwenzaixianyiquerqu.org.cn</code>
- <code>yazhouwuyejuchang.org.cn</code>

**备注**：上述域名均为示例数据，实际部署后需根据运营策略替换或补充为合法、合规且具备明确版权归属的资源地址。系统本身不存储或代理任何受版权保护的内容，仅作为导航元数据载体。

## 项目结构

```
cloudlink/
├── apps/
│   ├── web/                       # 前端 Next.js 应用（SSR + 静态生成）
│   │   ├── src/app/               # App Router 页面路由
│   │   ├── src/components/        # 可复用 UI 组件（Radix UI + Tailwind）
│   │   └── src/lib/               # 前端工具函数与 API 客户端
│   └── api/                       # 后端 Fastify 服务（RESTful API）
│       ├── src/routes/            # 路由控制器（按资源类型拆分）
│       ├── src/services/          # 业务逻辑层（链接、用户、统计、检查）
│       ├── src/workers/           # 后台任务（健康检查、快照抓取）
│       └── src/plugins/           # Fastify 插件（认证、数据库、缓存）
├── packages/
│   ├── db/                        # 数据库 Schema（Prisma）与迁移脚本
│   ├── config/                    # 共享配置（ESLint、Prettier、TypeScript）
│   └── utils/                     # 通用工具（日志、加密、HTTP 客户端）
├── docker/
│   ├── compose/                   # 开发与生产 Docker Compose 文件
│   ├── nginx/                     # Nginx 配置模板
│   └── scripts/                   # 容器初始化与健康检查脚本
├── docs/                          # 完整文档（见文档导航章节）
├── tests/                         # 单元测试、集成测试与 E2E 测试
├── .env.example                   # 环境变量参考文件
├── .gitignore                     # Git 忽略规则
├── package.json                   # 根 package（工作空间配置）
├── pnpm-workspace.yaml            # pnpm 工作空间定义
└── README.md                      # 本文件
```

## 贡献指南

我们欢迎社区提交各类贡献，包括但不限于代码修复、新功能、文档改进、翻译及资源推荐。请遵循以下步骤以确保协作顺畅。

1. **查阅路线图与议题**：访问 GitHub Issues 页面，查看 `help wanted` 或 `good first issue` 标签的任务，或在新议题中描述您希望解决的问题或功能建议，等待维护者反馈。
2. **派生仓库并创建分支**：将主仓库派生至个人账户，然后基于 `main` 分支创建一个描述性的新分支，例如 `feat/add-export-json` 或 `fix/health-check-timeout`。
3. **编写代码与测试**：遵循项目已配置的 ESLint 与 Prettier 规则编写代码。对于新功能或修复，需补充对应的单元测试（Jest）或集成测试（Supertest）。确保所有现有测试通过。
4. **提交变更并签署 DCO**：提交信息需遵循 Conventional Commits 格式。每个提交必须包含 `Signed-off-by` 行（通过 `git commit -s` 自动添加），以表明您同意开发者原创证书（DCO）协议。
5. **发起拉取请求**：推送到派生仓库后，向主仓库的 `main` 分支发起 Pull Request。请清晰描述变更内容、测试覆盖情况以及相关议题编号。维护者会在 3 个工作日内进行审查，可能要求修改或补充。

## 常见问题

**Q: 系统支持导入大量历史链接吗？例如我有上千个散落的书签需要迁移。**

A: 可以。系统提供了批量导入接口，支持 CSV 与 JSON 格式。您需要准备包含 `url`、`title`（可选）、`category`（可选）、`tags`（可选）的表格。同时，我们提供了一个命令行工具 `pnpm run cli:import -- --file=./bookmarks.csv`，该工具会异步处理导入任务，并生成导入报告（包括成功数、失败数及错误原因）。对于超大规模导入（>1万条），建议分批执行或通过后台任务队列提交。

**Q: 健康检查功能会影响源站性能吗？如何配置检查策略？**

A: 健康检查默认采用 HEAD 请求，仅获取响应头信息，不会下载页面完整内容，因此对源站负载影响极小。检查间隔、超时时间、重试次数以及 User-Agent 均可通过环境变量或管理后台配置。对于敏感或高负载站点，您可以将它们加入白名单，跳过自动检查，改为手动确认。所有检查结果均记录在 `link_health_audits` 表中，便于追溯。

**Q: 是否支持多语言界面？我能否自定义前端主题？**

A: 前端界面使用 Next.js 国际化路由，目前内置英文与简体中文。您可以通过在 `apps/web/src/i18n/locales/` 下添加新的 JSON 翻译文件来扩展其他语言。主题方面，项目采用 Tailwind CSS 并基于 CSS 变量设计色彩系统。您可以在 `apps/web/src/styles/theme.css` 中覆盖主色、字体、圆角等设计令牌，无需修改组件内部样式即可实现品牌化定制。

## 许可证

本项目采用 MIT 许可证。您可以自由使用、复制、修改、合并、出版发行、散布、再授权及销售软件副本，但需在软件的所有副本或重要部分中保留原始版权声明和许可声明。本软件按“现状”提供，不提供任何形式的明示或暗示担保，包括但不限于适销性、特定用途适用性和非侵权性。有关完整条款，请参阅项目根目录下的 LICENSE 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
