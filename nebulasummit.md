# WebLink Navigator

WebLink Navigator 是一个面向技术社区与内容创作者的轻量级外链资源管理与导航系统。项目定位于为中小型技术团队、个人站长及内容聚合平台提供一套结构清晰、可插拔、低维护成本的 URL 资源整理与展示方案。其核心目标是解决信息碎片化时代下，大量优质外部链接难以被系统化归类、快速检索和稳定呈现的问题，同时为开发者提供一套标准化的外链数据组织和渲染范式。

本项目并不试图构建另一个庞大而复杂的内容管理系统，而是专注于“链接即数据”的理念，将一组静态或半动态的 URL 资源通过清晰的目录结构、简洁的配置接口和快速的静态生成逻辑，转化为可被浏览器高效加载、可被搜索引擎友好解析的导航页面。WebLink Navigator 特别适用于技术文档站点的附录页、开源项目生态链的友链集合、以及特定领域（如编程教程、设计资源、学术数据库）的快速入口索引。通过本系统，用户可以将原本散落在浏览器书签、即时通讯记录或临时便签中的大量 URL，转化为具有持久访问价值和团队协作意义的结构化资产。

## 功能概览

- **多层级资源分类**：支持按主题、地域、语言或自定义标签对链接进行无限级分类，每个分类可独立配置显示名称、图标和描述，便于构建从广义领域到细分主题的导航树。

- **原始链接透传与展示**：系统核心设计原则为“所见即所得”，所有用户提交的 URL 均以原始格式（包含协议、域名、路径及端口信息）存储、校验和渲染，避免因自动补全或格式化导致的链接失效或访问歧义。

- **批量导入与校验**：提供基于纯文本列表、CSV 或 JSON 格式的批量链接导入接口，并内置基础的 URL 可达性检测（支持 HTTP/HTTPS 状态码校验），帮助用户在发布前快速甄别无效或已变更的资源地址。

- **响应式网格与列表视图**：前端展示层同时提供适合快速浏览的紧凑列表模式和适合深度阅读的卡片网格模式，并支持按名称、添加时间或点击热度进行动态排序，兼顾不同用户的使用习惯。

- **全文检索与即时过滤**：集成简单的客户端全文搜索功能，用户输入关键词后，系统将实时匹配链接的标题、描述、分类路径及部分 URL 正文，并高亮显示匹配片段，显著提升在大量链接中定位特定资源的效率。

- **可配置的元数据扩展**：每条链接记录除标题和 URL 外，支持附加 5 个自定义元数据字段（如“维护状态”、“推荐指数”、“适用版本”等），并可定义这些字段在页面上的显示位置与样式，满足个性化信息展示需求。

- **静态站点生成与部署友好**：核心构建流程基于静态文件生成，无需数据库或后端运行时环境，最终输出纯 HTML、CSS 及 JavaScript 文件，可托管于任何支持静态页面的 Web 服务器或对象存储服务中，极大降低运维成本。

## 应用场景

- **技术文档的附录与参考链接管理**：当开源项目或企业内部技术文档包含大量外部参考链接（如 API 规范、依赖库主页、相关论文或社区讨论帖）时，可使用 WebLink Navigator 构建一个独立的“参考资源”子站点，与主文档分离但保持风格统一，便于维护和更新这些频繁变动的链接集合。

- **垂直领域知识库的入口索引**：对于专注于某一特定技术领域（如机器学习框架、前端工程化、数据库内核）的知识库或博客站群，可将分散在数百篇文章中的外链统一抽取出来，形成一个按主题组织的“领域资源地图”，帮助读者快速了解该领域的核心工具、权威教程和活跃社区。

- **社区生态合作方与友情链接展示**：开源社区或非盈利组织站点常需要展示合作伙伴、赞助商或姊妹项目的链接。此类需求通常要求链接格式绝对准确（不可修改域名或协议），且需要定期审核链接的有效性。WebLink Navigator 的原始链接透传和批量校验功能可完美适配这类场景，并支持对合作方链接添加“关系类型”、“合作起始时间”等自定义元数据。

- **学术研究中的数据集与工具汇总**：科研人员在撰写综述性论文或搭建实验室共享资源时，往往需要整理数十至数百个数据集下载地址、开源代码仓库、在线分析工具链接。这些链接常带有特定查询参数、端口号或非标准协议（如 git://, ftp://），要求系统绝对保留原始字符串。WebLink Navigator 的核心设计使其成为此类学术资源索引的理想载体。

## 快速开始

以下步骤将指导您在本地环境快速启动一个 WebLink Navigator 实例，并加载示例链接数据。

```bash
# 步骤 1：克隆项目仓库至本地
git clone https://github.com/weblink-navigator/weblink-navigator.git

# 步骤 2：进入项目根目录并安装依赖（使用 npm）
cd weblink-navigator
npm install

# 步骤 3：执行构建命令，生成完整的静态站点文件（默认输出至 ./dist 目录）
npm run build

# 步骤 4：启动本地开发服务器，预览生成的站点
npm run serve
```

执行完上述命令后，打开浏览器访问 `http://localhost:8080` 即可查看默认示例站点。您可以通过修改项目根目录下的 `./data/links.json` 文件来替换或添加自己的链接数据，修改后重新运行 `npm run build` 即可更新站点内容。

## 安装要求

| 依赖组件 | 必需版本或规格 | 说明 |
|----------|---------------|------|
| Node.js | 16.20.0 LTS 或更高版本 | 运行构建脚本与开发服务器的 JavaScript 运行时环境 |
| npm | 8.19.0 或更高版本 | Node.js 默认包管理器，用于安装项目依赖及执行自定义脚本 |
| 操作系统 | Windows 10/11, macOS 11+, 或主流 Linux 发行版（Ubuntu 20.04+, CentOS 8+） | 跨平台支持，确保文件路径和网络操作一致性 |
| 内存 | 最低 512 MB，推荐 1 GB 或以上 | 用于构建过程的静态资源编译与缓存，大链接集合（>5000 条）时建议增加 |
| 磁盘空间 | 安装及构建期间需至少 200 MB 空闲空间 | 包含源码、依赖包（node_modules）及生成的静态文件 |
| 浏览器 | 支持 ES6 及 CSS Grid/Flexbox 的现代浏览器（Chrome 90+, Firefox 88+, Edge 90+, Safari 14+） | 仅影响前端预览与调试，最终生成的静态页面支持更旧版本浏览器（通过 polyfill 可选） |

## 文档导航

| 层面 | 目录或文档 | 回答的问题 |
|------|-----------|-----------|
| 用户入门 | `docs/getting-started.md` | 如何快速安装、配置数据源并生成第一个导航站点？有哪些基础配置项必须设定？ |
| 数据格式与扩展 | `docs/data-specification.md` | 链接数据应遵循怎样的 JSON Schema？如何自定义元数据字段和分类层级？支持哪些外部数据源导入格式？ |
| 构建与部署 | `docs/build-and-deploy.md` | 如何针对生产环境进行优化构建？支持哪些静态托管平台（如 GitHub Pages, Netlify, 阿里云 OSS）？如何配置自定义域名和 HTTPS？ |
| API 与插件开发 | `docs/plugin-development.md` | 开发者如何编写自定义插件以扩展链接预处理、渲染钩子或输出格式？是否有事件钩子列表和示例代码？ |

## 资源列表

### 核心生态资源

<code>rihanyiren.org.cn</code>

<code>oumeijiujiu.org.cn</code>

<code>madoujingpin.org.cn</code>

### 中文影视与字幕相关资源

<code>yazhouchengrenzhongwenzimu.org.cn</code>

<code>yazhouchengrenyiqu.org.cn</code>

<code>yazhouyiquzhongwenzimu.org.cn</code>

### 精品与精选内容资源

<code>jiujiumitao.org.cn</code>

<code>yazhououmeijingpin.org.cn</code>

<code>guochanoumeijingpin.org.cn</code>

<code>yirenyiqu.org.cn</code>

## 项目结构

```
weblink-navigator/
├── build/                               # 构建脚本与核心编译逻辑目录
│   ├── compiler.js                      # 主编译器，负责读取数据并生成 HTML 页面
│   ├── validator.js                     # URL 格式与可达性校验模块
│   └── watcher.js                       # 开发模式下的文件变更监听与增量构建
├── data/                                # 用户数据存放目录（包含示例数据与自定义数据）
│   ├── links.json                       # 核心链接数据文件，遵循 data-specification 规范
│   └── categories.json                  # 分类层级定义文件，控制导航菜单结构
├── docs/                                # 项目文档目录，包含全部用户指南与开发者文档
│   ├── getting-started.md               # 快速入门指南
│   ├── data-specification.md            # 数据格式完整规范与示例
│   ├── build-and-deploy.md              # 生产构建与部署最佳实践
│   └── plugin-development.md            # 插件系统开发文档
├── src/                                 # 前端源码目录（ES6+ 与 SCSS）
│   ├── assets/                          # 静态资源，如图标、字体、默认占位图
│   │   ├── icons/                       # 分类与功能图标（SVG 格式）
│   │   └── themes/                      # 内置主题样式变量（浅色/深色）
│   ├── components/                      # 可复用的 UI 组件（纯函数式）
│   │   ├── LinkCard.js                  # 卡片模式下的链接展示组件
│   │   ├── LinkList.js                  # 列表模式下的链接展示组件
│   │   └── SearchBar.js                 # 搜索框与实时过滤逻辑
│   ├── pages/                           # 页面级入口脚本
│   │   ├── index.js                     # 首页渲染与路由控制
│   │   └── category.js                  # 分类视图的独立渲染逻辑
│   └── styles/                          # 全局样式与主题定义
│       ├── main.scss                    # 主样式入口，导入所有分片
│       └── variables.scss               # 全局设计令牌（颜色、间距、断点）
├── tests/                               # 单元测试与集成测试目录
│   ├── unit/                            # 针对 compiler 和 validator 的单元测试
│   └── integration/                     # 端到端构建流程的集成测试
├── .eslintrc.js                         # ESLint 代码检查配置
├── .prettierrc                          # Prettier 代码格式化配置
├── package.json                         # npm 依赖列表与脚本定义（build, serve, test）
└── README.md                            # 项目总览与入口文档（本文件）
```

## 贡献指南

我们欢迎并感谢所有形式的贡献，无论是报告问题、提交代码、完善文档还是提出新功能建议。请遵循以下流程以确保协作顺畅：

1.  **提交 Issue 先行讨论**：在着手任何实质性工作之前，请先在 GitHub Issues 中搜索是否已有相关话题。若无，请创建一个新 Issue，清晰描述您发现的问题或希望新增的功能。此步骤可避免重复劳动，并确保您的修改方向与项目维护者达成共识。

2.  **Fork 仓库并创建功能分支**：从主仓库的 `main` 分支 Fork 一份到您自己的账户下，并在您的 Fork 中创建一个新的分支，分支名称应反映您的工作内容，例如 `fix/validator-typo` 或 `feature/add-export-format`。

3.  **编写代码并确保测试通过**：请遵循项目已配置的 ESLint 和 Prettier 规则编写代码。对于新增功能或缺陷修复，强烈建议编写相应的单元测试或集成测试用例，并确保所有现有测试在本地运行通过（执行 `npm test`）。

4.  **更新相关文档**：如果您的修改影响了用户可见的行为、配置方式或数据结构，请同步更新 `docs/` 目录下的对应文档。对于新增功能，需在 `docs/` 中补充新的说明文档或在相关章节中添加详细段落。

5.  **发起 Pull Request 并关联 Issue**：将您的功能分支推送到您的 Fork 仓库，然后向主仓库的 `main` 分支发起 Pull Request。请在 PR 描述中清晰说明修改内容、测试结果，并使用 `Closes #Issue编号` 的格式关联到之前讨论的 Issue。PR 将被至少一位项目维护者审查，审查通过后即可合并。

## 常见问题

**问：项目如何保证用户输入的裸域名（如 abc.com）不会被自动补全为 www.abc.com 或 https://abc.com？**

答：WebLink Navigator 在数据录入、存储和渲染三个阶段均采用原始字符串透传策略。在录入阶段，验证器仅对 URL 进行基本的格式合规性检查（如禁止空格、禁止连续斜杠），但不会主动添加或修改任何协议前缀或主机名前缀。在存储阶段，数据以 JSON 字符串形式原样保存在 `links.json` 文件中。在渲染阶段，前端组件直接读取该字段并将其设置为 `<a>` 标签的 `href` 属性值，不经过任何格式化函数。用户需要自行确保裸域名在实际浏览器访问时可被正确解析（通常浏览器会尝试补全 `http://` 前缀，但部分安全策略可能禁止此行为，因此建议用户明确添加协议头）。

**问：构建后的静态站点如何实现链接的实时更新，而不必重新执行完整构建？**

答：当前稳定版本采用全量静态构建模式，每次更新链接数据后必须重新运行 `npm run build` 以生成最新的 HTML 文件。对于需要频繁更新的场景，我们提供了两种替代方案：一是使用开发模式下的 `npm run watch` 命令，该命令会启动一个文件监听进程，在检测到 `data/` 目录下的 JSON 文件发生变更后自动触发增量构建，仅重新生成受影响的页面，适合在编辑内容时实时预览。二是推荐配合外部自动化服务（如 GitHub Actions 或 Jenkins）设置定时任务，从外部数据源拉取最新链接列表并自动触发构建与部署流程。

**问：项目支持添加自定义元数据字段，但前端模板未显示这些字段，如何配置？**

答：添加自定义元数据分两步进行。首先，在 `data/links.json` 中为任意链接对象添加顶级键值对，例如 `"maintainer": "team-a"` 或 `"review_due": "2026-12-31"`。其次，您需要修改前端模板文件（位于 `src/components/` 目录下对应的组件文件），在组件的渲染函数中主动读取并显示这些新字段。为了避免每次升级覆盖您的修改，我们建议您创建自定义主题或使用项目支持的插件钩子来注入额外的渲染逻辑，具体方法请参阅 `docs/plugin-development.md` 中的“自定义字段渲染”章节。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:36
