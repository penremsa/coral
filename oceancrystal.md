# NebulaLink 技术导航站

NebulaLink 是一个面向中文互联网技术从业者的外链资源导航与信息聚合系统，定位于为开发者、运维工程师、技术决策者以及科研人员提供高质量、低延迟、可验证的外部资源访问枢纽。项目目标用户包括需要频繁查阅赛事数据、实时比分、认证信息及区域性服务接口的技术团队，以及希望建立稳定外链引用体系的文档维护者。

本项目解决的核心问题在于：技术团队在构建信息看板、数据中台或运维监控系统时，常常面临外链来源分散、域名记忆成本高、可用性验证缺失等痛点。NebulaLink 通过集中化的外链登记、状态探测与访问日志记录，帮助用户在一个项目内完成对数十个关键域名的统一管理、健康检查与访问路由配置，显著降低外链维护的认知负担。

## 功能概览

- **外链资源登记与管理** 提供标准化的外链录入接口，支持批量导入、标签分类与备注添加，便于团队内部共享资源池。

- **多协议访问代理支持** 自动识别 HTTP 与 HTTPS 协议，支持配置本地代理规则，适配内网穿透、CDN 回源等复杂网络环境。

- **定时可用性探测** 内置基于 Cron 表达式的探测任务，定期向已登记域名发送 HEAD 请求，记录状态码与响应时间，异常时输出告警日志。

- **访问日志聚合分析** 记录每次外链访问的时间、来源 IP、User-Agent 及响应结果，提供简单的统计视图，辅助排查链路问题。

- **资源分组与权限隔离** 支持按项目、部门或环境（开发/测试/生产）对资源进行分组，并为不同用户角色配置只读或读写权限。

- **配置热加载与版本回溯** 资源列表变更后无需重启服务，通过 API 触发配置重载；同时保留最近 10 次配置快照，支持一键回滚。

- **开放 API 接口** 提供 RESTful API 供第三方系统调用，支持查询资源列表、触发探测、获取状态摘要，便于集成到现有运维平台。

## 应用场景

- **技术文档站外链维护** 技术团队在维护项目文档或 API 手册时，可将频繁引用的外部赛事数据、认证平台或统计服务域名统一录入 NebulaLink，当域名发生迁移或变更时，只需在导航站内更新一次，所有文档引用即可同步生效。

- **运维监控面板数据源管理** 运维人员利用 NebulaLink 集中管理监控面板中引用的第三方数据源地址，通过内置的探测功能定期验证各数据源的可达性，在数据源宕机或响应超时时第一时间发现并切换备选链路。

- **区域性服务聚合与测试** 对于需要访问特定区域（如 `.asia` 顶级域）服务的团队，可将多个区域性域名集中登记，通过 NebulaLink 的代理功能模拟不同地理位置的访问，验证服务在各地域的实际可用性。

- **赛事数据看板快速部署** 数据分析团队在搭建实时比分展示或赛事预测看板时，通过 NebulaLink 快速导入一组比赛数据相关域名，配合简单的路由规则即可将看板指向最新的数据来源，无需修改前端代码。

## 快速开始

以下步骤帮助您在本地环境中快速启动 NebulaLink 服务。

```bash
# 1. 克隆项目仓库
git clone https://github.com/nebulalink/nebulalink.git
cd nebulalink

# 2. 安装项目依赖（使用 pip 和 npm）
pip install -r requirements.txt
npm install --prefix frontend

# 3. 初始化配置文件并启动服务
cp config/example.yaml config/local.yaml
python scripts/init_db.py
python app.py --env local --port 8080
```

服务启动后，访问 `http://localhost:8080` 可查看 Web 管理界面。默认管理员账号为 `admin`，初始密码输出在控制台日志中，首次登录后请及时修改。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 - 3.12 | 后端核心运行环境，需包含 `sqlite3` 和 `ssl` 模块 |
| Node.js | 18.x 或 20.x LTS | 前端构建工具依赖，用于打包静态资源 |
| Redis | 7.0 及以上 | 用于探测任务的分布式锁和临时缓存，单机版即可 |
| SQLite | 3.35 及以上 | 默认元数据存储引擎，支持 WAL 模式提升并发性能 |
| Git | 2.30 及以上 | 用于版本控制和配置快照管理 |
| curl | 7.68 及以上 | 探测模块底层依赖，需支持 HTTPS 和超时参数 |
| cron | 系统自带 | 定时探测任务调度器，需确保 `crontab` 命令可用 |
| 系统内存 | 不低于 512 MB | 生产环境建议 2 GB 以上，用于缓存和日志缓冲 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `docs/user/quickstart.md` | 如何安装、登录、添加第一个外链资源？ |
| 用户手册 | `docs/user/resource-management.md` | 如何批量导入资源、配置分组、设置探测规则？ |
| 运维指南 | `docs/ops/deployment.md` | 如何在生产环境部署高可用实例，配置反向代理和 HTTPS 证书？ |
| 运维指南 | `docs/ops/monitoring.md` | 如何接入 Prometheus 监控，解读探测指标和告警规则？ |
| 开发者指南 | `docs/dev/api-reference.md` | API 接口的鉴权方式、请求参数和响应格式详解 |
| 开发者指南 | `docs/dev/contribute.md` | 代码风格、提交规范、PR 流程和测试用例编写要求 |
| 设计文档 | `docs/design/architecture.md` | 系统整体架构图、模块划分、数据流与扩展点设计 |

## 资源列表

本导航站登记的外部资源涵盖赛事数据、认证服务及区域性体育信息平台，所有链接均按类别整理如下。请注意保持原样使用，不添加额外前缀或修改协议。

### 篮球比分类

- <code>lanqiubifeng.org.cn</code>
- <code>lanqiubifenh.org.cn</code>

### 足球比分备用节点类

- <code>zuqiubifenziboa.org.cn</code>
- <code>zuqiubifenzibob.org.cn</code>
- <code>zuqiubifenziboc.org.cn</code>
- <code>zuqiubifenzibod.org.cn</code>
- <code>zuqiubifenziboe.org.cn</code>

### 亚洲区域赛事服务类

- <code>ajiasaicheng.asia</code>
- <code>bajiazhugongbang.asia</code>
- <code>baxizuqiujiajiliansai.asia</code>

## 项目结构

```
nebulalink/
├── app.py                         # 应用主入口，初始化 Flask 与调度器
├── config/
│   ├── default.yaml               # 默认配置模板（不可直接修改）
│   ├── example.yaml               # 完整示例配置，含所有参数说明
│   └── local.yaml                 # 本地环境配置文件（gitignore）
├── core/
│   ├── __init__.py
│   ├── resource_manager.py        # 资源增删改查及分组逻辑
│   ├── probe_engine.py            # 探测任务执行器，调用 curl 检测可用性
│   ├── log_collector.py           # 访问日志收集与简单统计聚合
│   └── config_loader.py           # 配置热加载与版本快照管理
├── api/
│   ├── __init__.py
│   ├── v1_routes.py               # RESTful API 路由定义（资源、探测、日志）
│   └── auth.py                    # JWT 鉴权与角色权限校验
├── frontend/                      # Vue 3 管理界面源码
│   ├── src/
│   │   ├── views/                 # 页面组件：资源列表、探测看板、日志查询
│   │   ├── store/                 # Pinia 状态管理，缓存资源分组与探测结果
│   │   └── api/                   # 前端 API 调用封装
│   └── dist/                      # 构建产物，由 nginx 或 Flask 静态托管
├── scripts/
│   ├── init_db.py                 # 初始化 SQLite 表结构及默认管理员账户
│   ├── import_batch.py            # 批量导入外链的工具脚本（支持 CSV/JSON）
│   └── backup_config.py           # 手动触发配置快照备份
├── tests/
│   ├── test_probe.py              # 探测引擎单元测试（模拟 curl 返回码）
│   ├── test_api.py                # API 接口功能及权限测试
│   └── test_config.py             # 配置加载与回滚逻辑测试
├── logs/                          # 运行日志与探测结果明细存储
├── requirements.txt               # Python 后端依赖清单
└── README.md                      # 项目说明文档
```

## 贡献指南

我们欢迎并感谢所有形式的贡献，包括但不限于代码、文档、测试用例和问题反馈。请遵循以下步骤参与项目开发。

1. **提交 Issue 讨论** 在提出 Pull Request 之前，请先在 GitHub Issues 中提交一个讨论帖，说明您希望解决的问题或新增的功能，避免重复劳动或方向偏离。

2. **Fork 仓库并创建特性分支** 将本项目 Fork 到您的个人账户下，然后基于 `develop` 分支创建一个新的特性分支，分支命名格式为 `feature/功能简述` 或 `fix/问题简述`。

3. **编写代码与测试** 完成代码实现后，请确保新增或修改的代码通过现有单元测试，并为新增功能补充对应的测试用例。代码风格需遵循 PEP 8 和 ESLint 规则。

4. **提交 Pull Request** 推送分支到您的 Fork 仓库后，向本项目的 `develop` 分支发起 Pull Request。PR 描述中需清晰说明变更内容、影响范围以及是否涉及配置变更。

5. **代码审查与合并** 项目维护者将在 3 个工作日内审查您的 PR，如无重大问题则予以合并。如需修改，维护者会在 PR 中留言反馈，请及时响应。

## 常见问题

**问：新增外链资源后，探测任务多久会生效？**

答：默认情况下，探测任务每 30 分钟执行一轮全量扫描。如果您需要立即触发探测，可以调用 API 接口 `POST /api/v1/probe/trigger` 或点击管理界面中的“立即探测”按钮，系统将异步启动一次补充扫描，结果在 1-2 分钟内更新至数据库。

**问：如果某个外链域名无法访问，系统会如何处理？**

答：探测引擎在连续 3 次探测失败（每次间隔 5 分钟）后，会将该资源标记为“异常”状态，并在日志中记录详细的错误信息（如超时、连接拒绝、SSL 证书错误）。同时，系统不会自动移除该资源，而是继续在后续周期中尝试探测，直至恢复后自动解除异常标记。您也可以在管理界面手动修改资源状态或设置忽略探测。

**问：能否将 NebulaLink 部署在无外网访问的内网环境？**

答：可以。但请注意，内网环境下的探测功能只能验证内网可直达的域名或 IP。对于需要外网访问的资源，您需要配置代理服务器，并在 `config/local.yaml` 中设置 `probe.proxy` 参数指向您的内网代理地址。此外，首次启动时初始化脚本会尝试连接公共 NTP 服务器校准时间，内网部署时请提前修改为本地时间源。

## 许可证

本项目采用 MIT 许可证。详细信息请参见项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
