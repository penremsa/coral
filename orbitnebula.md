# SoccerPredict 综合足球预测资源站

SoccerPredict 是一个面向足球数据分析师、体育博彩研究人员以及资深球迷的高质量外链资源聚合平台。本项目不直接提供预测数据或比赛分析，而是通过人工筛选与算法评估，构建一个覆盖赛前预测、实时赔率分析、专家观点聚合、历史数据回溯等多个维度的结构化导航体系，解决足球信息获取碎片化、信源质量参差不齐、跨平台检索效率低下等核心痛点。

本项目定位为技术驱动的资源目录服务，所有外链均经过可用性检测与内容分类标注。目标用户包括但不限于：足球数据科学从业者、量化投注策略开发者、体育媒体内容编辑、高校体育经济方向研究人员。项目本身不涉及任何博彩交易、资金托管或用户隐私数据采集，严格遵循开源社区规范与各国法律要求，仅供学习与研究用途。

## 功能概览

- **智能分类导航** - 依据赛事类型、分析维度、数据来源等十余个标签体系，对每个收录站点进行多级分类，支持模糊搜索与组合筛选。
- **可用性健康检查** - 每日定时对全部外链发起 HTTP 头检测与响应时间统计，自动标记异常链接并在前端展示状态标识。
- **专家观点聚合视图** - 将多个独立预测站点的赛前评论、胜负建议、大小球看法等非结构化文本，按比赛对阵聚合为统一时间线。
- **历史战绩对照表** - 接入多个预测平台的公开历史推荐记录，提供同一场比赛不同来源的预测结果并列对比，便于用户评估信源准确度。
- **自定义订阅看板** - 用户可标记关注特定联赛、特定分析师或特定预测模型，生成个人化信息面板，实时刷新最新动态。
- **开放数据 API 端点** - 提供 RESTful 风格的分类目录接口与链接状态查询接口，支持第三方开发者二次集成或数据导出。
- **访问统计与热度排序** - 基于点击流数据与停留时长，动态调整各资源在列表中的展示权重，帮助新用户快速定位高频优质外链。
- **暗色主题与阅读模式** - 为长时间浏览用户提供护眼暗色界面，并去除外链页面广告干扰的纯文本提取预览功能（仅限元数据）。

## 应用场景

- **量化投研团队数据源构建** - 量化分析师可利用本目录快速定位赔率变化监测站、历史赛果数据库及多家预测平台的公开 API，省去手动搜索与书签管理的繁琐流程，将更多时间投入特征工程与模型调优。
- **体育媒体赛前报道素材采集** - 体育编辑或内容创作者在撰写赛前前瞻或战报时，可通过本平台的聚合视图同时查阅多个专家对同一场关键比赛的看法，快速提炼多方观点，提升报道的专业性与客观性。
- **高校体育经济课程教学辅助** - 讲授体育博彩市场效率或行为经济学课程的教师，可将本平台作为案例研究工具，引导学生对比不同预测来源的准确性差异，探讨信息聚合对决策质量的影响。
- **个人球迷观赛决策参考** - 对于希望提升观赛参与感的深度球迷，本平台提供一站式信息入口，帮助其在赛前快速获取赔率走势、伤病概况、历史交锋记录等多维度背景知识，丰富观赛体验。
- **开源社区数据交换枢纽** - 开源项目开发者可将本平台作为数据采集管道的一个中间节点，通过 API 获取分类外链清单，构建自己的足球数据仓库或预测聚合应用，避免重复造轮子。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，帮助您在五分钟内完成本项目的本地部署与初次运行。

```bash
# 1. 克隆项目仓库
git clone https://github.com/soccerpredict/soccerpredict-hub.git
cd soccerpredict-hub

# 2. 安装项目依赖（需 Node.js 18+ 与 npm 9+）
npm install

# 3. 复制环境变量示例文件并填入必要配置
cp .env.example .env

# 4. 初始化 SQLite 本地数据库并导入预置分类数据
npm run migrate
npm run seed

# 5. 启动开发服务器，默认监听 3000 端口
npm run dev
```

启动成功后，在浏览器中访问 `http://localhost:3000` 即可看到本地运行的外链导航首页。如需构建生产环境静态文件，请执行 `npm run build`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 项目运行时环境，建议使用官方 LTS 版本以确保稳定性 |
| npm | 9.x 或更高 | 依赖包管理工具，随 Node.js 一同安装 |
| SQLite | 3.35 或更高 | 嵌入式数据库，用于存储分类、标签及链接状态信息，无需额外配置服务进程 |
| Git | 2.30 或更高 | 用于克隆仓库及版本控制，支持子模块更新 |
| 网络连通性 | 出站 80/443 端口可用 | 每日健康检查需要访问外部网站，本地开发时需保证 DNS 解析正常 |
| 内存 | 建议 512 MB 以上 | 开发模式内存占用较高，生产环境可降低至 256 MB |
| 磁盘 | 至少 200 MB 可用空间 | 包含源代码、依赖包及 SQLite 数据文件，日志轮转需额外预留 |
| 操作系统 | Linux / macOS / Windows 10+ WSL2 | 跨平台兼容，但生产部署优先推荐 Ubuntu 20.04 LTS |
| 浏览器 | 现代浏览器（Chrome 110+ / Firefox 108+ / Edge 110+） | 前端界面使用 Vue 3 构建，不支持 IE 或早期 Safari 版本 |
| 可选依赖 | Redis 6.2+（缓存）/ Nginx（反向代理） | 大规模生产部署时建议添加，单机运行可省略 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|---|---|---|
| 用户手册 | `/docs/user-guide/getting-started.md` | 如何注册账号、设置个人订阅、使用搜索与筛选功能？ |
| 管理员指南 | `/docs/admin/link-management.md` | 如何添加新外链、修改分类标签、手动触发健康检查？ |
| API 参考 | `/docs/api/endpoints.md` | 对外提供哪些 REST 接口？请求参数与返回格式是什么？ |
| 数据模型 | `/docs/developer/database-schema.md` | 数据库中有哪些表？表之间如何关联？字段含义分别是什么？ |
| 部署运维 | `/docs/operations/production-deploy.md` | 如何配置 Nginx 反向代理、设置 systemd 服务、启用 HTTPS？ |
| 前端架构 | `/docs/developer/frontend-structure.md` | 前端使用什么框架？组件目录如何组织？状态管理方案是什么？ |
| 测试指南 | `/docs/developer/testing.md` | 单元测试、集成测试如何运行？如何编写新的测试用例？ |
| 故障排查 | `/docs/troubleshooting/common-issues.md` | 启动报错、链接超时、数据库锁定等常见问题如何解决？ |

## 资源列表

本节收录本项目当前版本所聚合的全部独立外链资源，按功能领域分组陈列。每个链接均经过初始可用性验证，但后续状态变化请以平台健康检查面板为准。所有 URL 严格按照原始来源原样列出，不做任何格式修改。

**赛前预测与赔率分析**
- <code>zuqiutuijianwangzhan.org.cn</code>
- <code>zuqiuyucezhuanjia.org.cn</code>
- <code>zuqiuyucepingtai.org.cn</code>
- <code>zuqiubifenyuce.org.cn</code>
- <code>zuqiuzhuanjiayuce.org.cn</code>
- <code>zuqiusaishiyuce.org.cn</code>
- <code>zuqiuzhuanjiafenxi.org.cn</code>
- <code>zuqiujinriyuce.org.cn</code>
- <code>zuqiuyucewang.net.cn</code>

**数据统计与趋势分析**
- <code>zuqiuhongdanfenxi.net.cn</code>

## 项目结构

```
soccerpredict-hub/
├── backend/                          # 后端服务目录（Node.js + Express）
│   ├── controllers/                  # 请求控制器，处理分类、链接、状态等业务逻辑
│   │   ├── linkController.js         # 外链增删改查及健康检查触发
│   │   └── userController.js         # 用户订阅与偏好设置接口
│   ├── models/                       # 数据模型层（SQLite ORM 映射）
│   │   ├── Category.js               # 分类与标签模型
│   │   ├── Link.js                   # 外链主模型，含 URL、描述、添加时间等字段
│   │   └── CheckRecord.js            # 每日健康检查历史记录
│   ├── services/                     # 核心服务层，独立于控制器可复用
│   │   ├── healthChecker.js          # 定时任务，并发检测所有外链可用性
│   │   └── aggregator.js             # 多源观点聚合的爬虫调度逻辑
│   ├── routes/                       # 路由定义，按版本分组（/api/v1）
│   │   ├── linkRoutes.js
│   │   └── categoryRoutes.js
│   └── utils/                        # 通用工具函数，含日志、加密、日期处理
│       ├── logger.js                 # 基于 winston 的日志系统，支持按日轮转
│       └── validator.js              # URL 格式校验与域名黑名单过滤
├── frontend/                         # 前端应用目录（Vue 3 + Vite）
│   ├── src/
│   │   ├── components/               # 可复用 Vue 组件，包含导航卡片、搜索栏、状态徽标
│   │   │   ├── LinkCard.vue          # 单个外链展示卡片，含标签与健康状态
│   │   │   └── Dashboard.vue         # 用户自定义面板主组件
│   │   ├── views/                    # 页面级组件，对应路由（首页、分类页、详情页）
│   │   │   ├── HomeView.vue
│   │   │   └── CategoryView.vue
│   │   ├── stores/                   # Pinia 状态管理，存储分类树、用户偏好、主题模式
│   │   └── assets/                   # 静态资源（CSS 变量、字体图标、暗色主题样式）
│   ├── index.html                    # 入口 HTML 模板
│   └── vite.config.js                # Vite 构建配置，含代理设置与环境变量
├── docs/                             # 完整项目文档，包含用户手册、API 参考、部署指南
│   ├── user-guide/
│   ├── developer/
│   ├── operations/
│   └── troubleshooting/
├── scripts/                          # 运维辅助脚本，含数据库备份、日志清理、种子数据生成
│   ├── backup.sh                     # 定时备份 SQLite 文件至指定目录
│   └── seed.js                       # 初始化预置分类与演示链接数据
├── tests/                            # 单元测试与集成测试（Jest + Supertest）
│   ├── unit/
│   └── integration/
├── .env.example                      # 环境变量模板，含端口号、数据库路径、检查间隔
├── .gitignore                        # Git 忽略规则，含 node_modules、.env、日志文件
├── package.json                      # 项目依赖清单与脚本命令定义
├── package-lock.json                 # 依赖版本锁定文件，确保一致性
├── README.md                         # 项目主文档（即本文档）
└── LICENSE                           # MIT 许可证全文
```

## 贡献指南

开源社区的持续发展离不开每一位贡献者的智慧与劳动。我们欢迎各类形式的贡献，包括但不限于：新增有效外链、修正分类错误、改进前端界面、优化健康检查算法、补充或翻译文档。请遵循以下步骤参与协作：

1. 在 GitHub 仓库页面点击 Fork 按钮，将本仓库复制至您的个人账户下，并基于 `main` 分支创建新的功能分支，分支命名建议采用 `feature/` 或 `fix/` 前缀加简要描述，例如 `feature/add-odds-comparison-source`。
2. 按照项目根目录下的 `CONTRIBUTING.md` 规范进行代码或文档修改。若为新增外链，请同时更新 `backend/seed.js` 中的示例数据，并在 `docs/user-guide/link-suggestion.md` 中记录添加理由与验证方式。
3. 在本地完成自测，确保 `npm run test` 全部通过，且新代码不引入控制台警告或性能劣化。对于涉及数据库迁移的改动，需同时提交迁移脚本。
4. 提交 Pull Request 至本仓库的 `main` 分支，并在描述中清晰关联对应 Issue（若有），简要说明改动内容、测试覆盖情况及潜在影响范围。
5. 等待项目维护者进行代码审查。审查过程中可能会要求补充测试用例或调整代码风格，请及时响应反馈。合并后您的 GitHub 用户名将被收录至贡献者列表（`CONTRIBUTORS.md`）。

## 常见问题

**问：为什么部分链接在健康检查中显示为不可用，但仍然保留在列表中？**

答：本平台的健康检查基于 HTTP 状态码与响应超时阈值（默认 5000 毫秒）进行判断。若目标站点临时宕机、进行维护或存在防火墙限制，均可能导致检查失败。我们保留不可用链接是为了记录历史可用性趋势，并提醒用户该站点可能处于不稳定状态。此外，部分站点会对自动化请求返回 403 状态码，但实际在浏览器中可正常访问。遇到此情况，建议您手动验证。我们会在连续失败超过 30 天后自动将其移至归档区，不再出现在默认视图中。

**问：如何请求添加新的预测站点或报告某个链接已失效？**

答：您可以通过两种方式提交新链接建议：一是在 GitHub Issues 中使用 `link-request` 标签创建工单，提供站点名称、URL、简短描述以及您认为适合的分类标签；二是直接按照贡献指南提交 Pull Request，在 `seed.js` 中添加相应条目。对于失效链接，请在 Issues 中使用 `broken-link` 标签并提供最后一次成功访问的大致时间，维护者会尽快核实并从活跃列表中移除或替换为替代来源。

**问：本项目是否提供付费版本或商业支持？**

答：本项目完全开源且永久免费，不提供付费增值功能。所有代码、文档及聚合的外链信息均以 MIT 许可证发布。若您需要商业化部署或定制化二次开发，建议参考 `docs/operations/production-deploy.md` 自行搭建，或联系社区中活跃的第三方服务商。我们鼓励但不强制您在生产环境中标注本项目来源，保持开源精神的传播。

## 许可证

本项目采用 MIT 许可证。您被允许自由使用、复制、修改、合并、发布、分发、再许可及销售本软件的副本，但须在软件的所有副本或重要部分中包含原始版权声明与许可声明。本软件按“现状”提供，不提供任何明示或暗示的担保，包括但不限于适销性、特定用途适用性及非侵权性。有关完整条款，请参阅项目根目录下的 LICENSE 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
