# Nebula Index

Nebula Index 是一个面向技术调研者、内容安全研究员与开源社区治理人员的轻量级外链资产整理与分类导航工具。该项目不对任何外部资源进行内容搬运或镜像存储，仅提供基于域名类别与访问稳定性的结构化索引能力，帮助用户在大规模外链整理场景中快速建立分类认知、降低重复确认成本。

项目定位为技术辅助工具，而非内容聚合平台。其核心价值在于：将零散的、高重复性的外链确认工作转化为可批量执行的本地脚本流程，配合静态页面生成方案，使外链索引表可直接内嵌至团队内部文档系统或本地知识库。目标用户包括社区运营人员、合规审计助理、爬虫策略工程师以及需要定期复核域名可达性的技术文档维护者。

## 功能概览

- **批量域名可达性检测**：对索引表中的全部域名执行轻量级 HEAD 请求，记录状态码与响应时间，辅助判断资源可用性。

- **分类标签手动管理**：允许用户为每个条目添加自定义分类标签，例如“成人内容”、“社区平台”、“工具站”等，便于后续筛选与统计。

- **结构化索引表导出**：支持将域名列表与附加信息导出为 Markdown 表格、JSON 或 CSV 格式，方便导入其他数据处理流水线。

- **变更历史快照对比**：每次运行脚本时自动生成索引快照，对比两次运行之间的域名增删与状态变化，输出差异报告。

- **本地静态预览服务**：内置基于 Python http.server 的静态预览模式，可在本地启动轻量网页，展示当前索引表的分组视图。

- **域名格式校验与清洗**：自动检测用户输入的域名是否包含非法字符、是否缺失协议头，给出规范化建议并记录日志。

- **注释与备注字段支持**：每个域名条目均可附带不超过 200 字的备注文本，用于记录确认时间、确认人、特殊访问注意事项等。

- **批量导入与合并**：支持从文本文件或 CSV 中批量导入新域名，与现有索引自动去重合并，并标记新增来源。

## 应用场景

- **内容安全团队日常复核**：团队成员每周使用 Nebula Index 对特定分类下的所有域名进行可达性检查，将不可达或响应异常的条目筛选出来，提交给运维同事进一步处理，减少人工逐条粘贴 URL 的重复劳动。

- **开源项目外链依赖梳理**：某开源文档项目在其 README 中引用了超过 50 个外部链接，维护者使用本项目的脚本提取所有链接域名，生成分类索引表，并定期检测这些域名是否仍处于活跃状态，避免文档中出现大量死链。

- **爬虫策略域名预热**：爬虫开发者在部署新采集任务前，将目标域名列表导入 Nebula Index，通过批量检测获取响应时间分布，初步评估目标服务的稳定性，为超时时间和重试策略提供参考数据。

- **个人知识库外部资源归档**：研究员在整理某一课题的公开资料时，将收集到的各类外部网站域名统一录入 Nebula Index，添加研究领域标签和简短备注，形成可检索、可追溯的外部资源目录。

- **域名到期与迁移预警辅助**：运维人员将重要业务依赖的第三方域名纳入索引，定期执行快照对比，当发现域名解析变更或证书信息变化时，及时通知相关业务方进行评估。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，Python 版本要求 3.8 及以上。

```bash
# 克隆项目仓库
git clone https://github.com/nebula-index/nebula-index.git
cd nebula-index

# 安装依赖（推荐使用虚拟环境）
python3 -m venv venv
source venv/bin/activate  # Windows 下使用 venv\Scripts\activate
pip install -r requirements.txt

# 运行初始索引构建脚本
python scripts/build_index.py --input data/raw_urls.txt --output dist/index.json

# 启动本地静态预览
python scripts/serve.py --port 8080
```

执行完成后，打开浏览器访问 `http://127.0.0.1:8080` 即可查看当前索引的静态表格视图。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|----------|----------|------|
| Python | 3.8 或更高 | 核心运行环境，用于执行所有脚本与逻辑处理 |
| pip | 21.0 或更高 | Python 包管理工具，用于安装 requirements.txt 中列出的依赖 |
| requests | 2.28.0 或更高 | 用于发送 HTTP 请求，执行可达性检测与状态码获取 |
| click | 8.1.0 或更高 | 命令行参数解析库，为所有脚本提供统一的 CLI 交互界面 |
| markdown | 3.4.0 或更高 | 用于将索引数据渲染为 Markdown 表格格式的输出文件 |
| colorama | 0.4.6 或更高 | 终端彩色日志输出，提升命令行反馈的可读性（Windows 下同样支持） |
| pytest | 7.2.0 或更高 | 仅开发与测试阶段使用，用于执行单元测试与集成测试用例 |

## 文档导航

| 层面 | 目录 / 文件 | 回答的问题 |
|------|-------------|------------|
| 用户手册 | docs/user_guide.md | 如何安装、配置、运行索引构建与预览服务；如何管理自定义分类标签 |
| 开发者指南 | docs/developer_guide.md | 项目代码结构说明、核心模块 API 文档、新增检测协议（如 TCP / UDP）的扩展方法 |
| 运维参考 | docs/operations.md | 如何将索引输出集成至 CI/CD 流水线、如何配置定时检测任务、日志轮转策略 |
| 设计说明 | docs/design.md | 索引数据模型设计、快照对比算法描述、静态预览页面的渲染逻辑与前端依赖说明 |

## 资源列表

### 原始数据索引条目（第 329/455 批）

以下 URL 均为用户原始输入数据，按原样收录，未做任何格式修改或规范化处理。每个条目均保留原始协议与主机名形式，供后续脚本进行校验与分类标记。

<code>sihuchengrenwangzhi.org.cn</code>

<code>zhongwenzimusiwazhifu.org.cn</code>

<code>mianfeishipinyiquerqu.org.cn</code>

<code>oumeijiqingzipai.org.cn</code>

<code>oumeiheirencuda.org.cn</code>

<code>rennicaoshipin.org.cn</code>

<code>qingqingcaoqingyule.org.cn</code>

<code>wuyenannvshuangshuangshuang.org.cn</code>

<code>renqidiyiye.org.cn</code>

<code>daxiangjiaoyazhou.org.cn</code>

## 项目结构

```
nebula-index/
├── data/                                 # 数据目录，存放原始输入与中间产物
│   ├── raw_urls.txt                      # 用户导入的原始 URL 列表，每行一个
│   └── categories.yaml                   # 用户自定义分类标签映射表
├── scripts/                              # 可执行脚本集合
│   ├── build_index.py                    # 主索引构建脚本，读取 raw_urls.txt 并输出 JSON
│   ├── serve.py                          # 启动本地静态预览服务的辅助脚本
│   ├── snapshot_diff.py                  # 快照对比工具，输出两次索引之间的变化
│   └── validate_domains.py               # 域名格式校验与清洗工具，支持批量修复建议
├── src/                                  # 核心 Python 源码模块
│   ├── checker.py                        # HTTP 可达性检测器，封装 requests 与超时策略
│   ├── indexer.py                        # 索引数据结构管理，包含增删改查与序列化
│   ├── formatter.py                      # 输出格式转换器，支持 Markdown / JSON / CSV
│   └── snapshot.py                       # 快照生成与对比逻辑，基于哈希比对
├── tests/                                # 单元测试与集成测试用例
│   ├── test_checker.py                   # 针对 checker 模块的模拟网络测试
│   ├── test_indexer.py                   # 索引数据结构的增删改查测试
│   └── fixtures/                         # 测试用固定数据集，包含模拟域名列表
├── dist/                                 # 构建输出目录，存放生成的索引文件与静态页面
│   ├── index.json                        # 完整索引数据的 JSON 格式输出
│   ├── index.md                          # 索引数据的 Markdown 表格版本
│   └── static/                           # 静态预览页面的 HTML / CSS / JS 资源
├── docs/                                 # 项目文档目录，包含用户手册与开发者指南
├── requirements.txt                      # 运行环境所需 Python 依赖列表
├── setup.py                              # 项目安装脚本，支持 pip install -e 模式
└── README.md                             # 项目说明文档（本文件）
```

## 贡献指南

1. **报告问题或建议功能**：请在 GitHub Issues 页面提交新议题，使用提供的模板填写当前行为、期望行为以及最小复现步骤。对于功能建议，请明确描述使用场景和预期收益。

2. **本地开发环境搭建**：Fork 本项目后，在本地执行 `pip install -e .[dev]` 安装开发模式依赖，并运行 `pytest tests/` 确认所有测试用例通过，确保环境配置正确。

3. **提交代码变更**：创建新的功能分支，命名格式为 `feature/简述变更内容` 或 `fix/简述修复内容`。提交前执行 `make lint` 和 `make test` 确保代码风格与测试均通过。所有提交信息应遵循 Conventional Commits 规范。

4. **更新索引数据条目**：若需新增或修正资源列表中的域名，请直接编辑 `data/raw_urls.txt` 文件，并附带说明变更原因。不建议直接修改 `dist/` 目录下的输出文件，应通过运行 `build_index.py` 重新生成。

5. **完善文档**：对 README、用户手册或开发者指南的修正，请直接在对应 `.md` 文件中修改，并确保文档中的命令示例可复现。涉及 URL 的改动必须严格保持原始格式。

## 常见问题

**问：项目是否会对所列域名进行内容抓取或存储页面副本？**

答：不会。Nebula Index 仅执行轻量级 HEAD 请求或 TCP 连接检测，用于判断域名是否可达及响应状态。项目不下载页面内容，不存储页面快照，也不分析页面文本。所有检测结果仅包含状态码、响应时间和解析 IP 信息。

**问：如何批量添加新的域名到索引中？**

答：将新域名逐行追加至 `data/raw_urls.txt` 文件中，每行一个域名，保持原始格式（不添加协议头，除非原数据包含协议头）。保存文件后重新运行 `python scripts/build_index.py`，脚本会自动去重并生成更新后的索引文件。若需添加备注信息，可同时修改 `data/categories.yaml` 中的对应条目。

**问：静态预览页面无法显示某些域名的状态信息，是什么原因？**

答：静态预览页面仅展示上次运行 `build_index.py` 时已保存的快照数据。如果域名状态未更新，请重新执行构建脚本。另外，部分网络环境可能对外部域名发起请求有限制，建议在服务器或本地网络策略允许的主机上运行检测脚本。预览页面本身不发起跨域请求，所有数据均来自本地 `index.json` 文件。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:31
