# ResourceHub

ResourceHub 是一个面向开发者与技术研究人员的轻量级外链资源聚合与导航系统。项目定位于对互联网上分散的高价值技术内容、工具站点、文档门户及社区平台进行结构化整理与分类归档，通过简洁的索引页面与可扩展的目录体系，帮助用户快速定位所需资源，减少信息检索成本。

项目本身不存储或托管任何第三方内容，仅提供 URL 元数据整理与分类展示功能。适用于个人知识管理、团队技术文档辅助、开源项目外部引用索引等场景。ResourceHub 以静态站点形式部署，支持通过 JSON 或 YAML 格式导入导出资源列表，便于二次开发与自动化更新。

## 功能概览

- **多级分类导航**：支持按技术领域、资源类型、适用人群等维度建立分类树，每个资源可归属于多个标签体系。
- **URL 原样登记系统**：所有外链资源强制以原始字符串形式录入，保留协议、域名层级及路径大小写，确保跳转准确无误。
- **快速检索过滤**：内置基于标题、标签、描述字段的简易文本搜索，支持多关键词组合筛选。
- **批量导入导出**：提供 CSV 与 JSON 格式的数据迁移接口，便于从旧系统迁移或与团队同步资源清单。
- **资源可用性自检**：支持配置定时任务对已登记 URL 进行 HTTP 状态码探测，自动标记异常链接。
- **访问统计看板**：记录各资源被点击的次数及最后访问时间，辅助评估资源热度。
- **私有标签备注**：允许用户为每个资源添加仅本地可见的备注信息，不随公开数据同步。
- **响应式布局主题**：适配桌面端与移动端浏览，内置浅色/深色两种界面主题，跟随系统偏好自动切换。

## 应用场景

1. **技术团队内部文档辅助**：开发团队可将常用的 API 文档、设计规范、运维手册、CI/CD 工具地址统一收录至 ResourceHub，新成员入职时通过导航页快速熟悉工具链，减少重复询问。

2. **开源项目外部引用索引**：开源项目维护者可在 README 中引用 ResourceHub 作为补充资源页，将项目依赖的协议解读、最佳实践案例、社区讨论帖等长尾链接集中管理，避免主文档过于冗长。

3. **个人知识库外链归档**：研究人员或博客作者在整理技术笔记时，可将散落在浏览器书签中的数百个外链按主题导出至 ResourceHub，配合标签与备注构建可检索的私人信息库。

4. **技术社区资源共建**：技术社区或培训机构可使用 ResourceHub 搭建公开的资源导航站，由成员共同提交优质链接，经审核后发布，形成领域内的垂直入口。

5. **离线环境模拟测试**：在内部隔离网络中，可通过 ResourceHub 的导入导出功能，将外网资源清单批量导入至镜像环境，辅助进行合规性审查或访问策略模拟。

## 快速开始

以下命令适用于 Linux / macOS / Windows WSL 环境。请确保已安装 Git 与 Node.js（版本 >= 18.x）。

```bash
# 克隆项目仓库
git clone https://github.com/resource-hub/core.git resource-hub
cd resource-hub

# 安装依赖
npm install

# 以开发模式运行本地服务，默认监听端口 3000
npm run dev
```

启动成功后，在浏览器中访问 <code>http://localhost:3000</code> 即可查看导航页面。首次运行将自动生成示例分类与占位数据，可通过管理界面或直接编辑 `./data/sources.json` 文件更新资源列表。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|---|---|---|
| Node.js | >= 18.0.0 | 运行时环境，用于执行构建脚本与开发服务器 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖及运行脚本命令 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库及后续更新拉取 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 推荐 Unix-like 环境，Windows 下建议使用 WSL2 以避免文件路径问题 |
| 浏览器 | 支持 ES2022 的现代浏览器 | 推荐 Chrome 108+ / Firefox 115+ / Edge 108+，用于前端界面渲染 |
| 磁盘空间 | >= 200 MB | 包含源代码、依赖包及构建缓存，实际生产部署可缩减至 50 MB |
| 网络访问 | 外网出站权限 | 用于首次构建时下载 npm 包及后续资源状态自检功能 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | /docs/user-guide/ | 如何添加分类、录入资源、编辑备注、切换主题以及导入导出数据 |
| 管理员指南 | /docs/admin-guide/ | 如何配置资源自检周期、管理用户权限、备份数据以及迁移服务器 |
| 开发者文档 | /docs/developer-guide/ | 项目目录结构说明、核心数据流、API 接口定义、插件扩展机制及二次开发规范 |
| 部署参考 | /docs/deployment/ | 支持静态生成、Docker 容器化、Nginx 反向代理、Vercel/Netlify 一键部署等方案的具体配置 |

完整文档请于项目根目录下的 `/docs` 文件夹中查看，或通过运行 `npm run docs:serve` 启动本地文档站点。

## 资源列表

本导航项目收录的资源按主题分为若干类别，所有链接均以原始字符串形式登记，确保跳转路径与来源完全一致。

**影视与娱乐类**

- <code>rihanrenqixilie.org.cn</code>
- <code>shibajinzaixianmianfeiguankan.org.cn</code>
- <code>shunvtiantang.org.cn</code>
- <code>yazhoupapa.org.cn</code>
- <code>yeyejiujiu.org.cn</code>
- <code>oumeizipaiqu.org.cn</code>
- <code>wuyeneishe.org.cn</code>
- <code>jiujiujiujiuguochan.org.cn</code>
- <code>renqixiliezhongwenzimu.org.cn</code>
- <code>neishemama.org.cn</code>

以上链接均按照用户提供的原始格式登记，未做任何协议补全、域名规范化或大小写修改。实际访问时请根据目标站点的实际部署情况自行补充协议头。

## 项目结构

```
resource-hub/
├── config/                         # 环境配置文件目录
│   ├── default.json               # 默认端口、缓存时间、分页大小等全局参数
│   └── custom.yaml                # 用户自定义配置覆盖项，不提交至版本库
├── src/
│   ├── core/                      # 核心数据模型与业务逻辑
│   │   ├── registry.js           # 资源注册与去重校验实现
│   │   ├── categoryTree.js       # 分类树构建与层级遍历算法
│   │   └── healthCheck.js        # HTTP 状态探测与超时重试策略
│   ├── routes/                    # 路由处理层
│   │   ├── api.js                # 资源增删改查的 RESTful 接口
│   │   └── page.js               # 服务端渲染页面路由
│   ├── views/                     # 前端模板与静态资源
│   │   ├── layouts/              # 基础页面骨架模板
│   │   ├── components/           # 可复用卡片、列表、搜索框等组件
│   │   └── assets/               # CSS 样式、JavaScript 脚本及图标字体
│   └── utils/                     # 通用工具函数
│       ├── validator.js          # URL 格式校验与域名黑名单过滤
│       └── exporter.js           # JSON / CSV 导出器
├── data/                          # 业务数据存储（默认以 JSON 文件模拟数据库）
│   ├── sources.json              # 主资源列表，含标题、URL、标签、备注字段
│   └── categories.json           # 分类定义，含层级关系与显示顺序
├── tests/                         # 单元测试与集成测试脚本
│   ├── registry.test.js
│   └── healthCheck.test.js
├── docs/                          # 完整文档目录，包含用户手册与开发指南
├── scripts/                       # 辅助运维脚本
│   ├── import-csv.js             # 从外部 CSV 批量导入资源
│   └── auto-check.js             # 定时执行资源可用性扫描
├── Dockerfile                     # 容器化构建定义，基于 node:18-alpine
├── package.json                   # 项目依赖清单及 npm 脚本入口
├── README.md                      # 项目概览文档（即本文档）
└── LICENSE                        # MIT 许可证全文
```

## 贡献指南

我们欢迎并鼓励社区提交各类改进，包括但不限于新增分类模板、优化检索算法、补充文档翻译及修复缺陷。请遵循以下步骤参与贡献：

1. **查阅现有议题**：访问项目 GitHub Issues 页面，确认您要解决的问题或提议的功能是否已被他人提出。若存在相关议题，请在其中留言表达参与意愿。

2. **派生项目仓库**：点击项目页面右上角的 Fork 按钮，将仓库派生至您的个人账号下，随后克隆该派生副本至本地开发环境。

3. **创建特性分支**：在本地仓库中执行 `git checkout -b feature/your-feature-name` 创建独立分支，避免在主分支上直接修改。建议分支名称简洁描述改动内容，例如 `fix-import-csv-encoding`。

4. **编写测试与代码**：若为功能性改动，请补充对应的单元测试用例（位于 `/tests` 目录），并确保所有现有测试通过。代码风格遵循项目内配置的 ESLint 规则，运行 `npm run lint` 可进行检查。

5. **提交合并请求**：完成改动后，将分支推送至您的派生仓库，随后在原始项目中创建 Pull Request。请清晰描述改动目的、实现方式及影响范围，并关联相关议题编号。核心维护者将在 5 个工作日内进行审阅。

## 常见问题

**Q：ResourceHub 是否存储或缓存第三方内容？**

A：不存储。项目仅记录用户提交的 URL 字符串、标题及分类元信息，不缓存页面内容、不保存快照、不代理请求。所有资源跳转由浏览器直接发起，原始内容仍由目标站点托管。资源可用性自检功能仅返回 HTTP 状态码，不下载主体数据。

**Q：如何批量更新资源列表？是否支持数据库？**

A：默认使用 `/data/sources.json` 存储数据，适合中小规模场景。您可以通过 `npm run import` 命令导入 CSV 文件覆盖或追加数据。对于大规模生产环境，项目提供了适配 PostgreSQL 的存储适配器扩展包，可在 `/docs/developer-guide/database.md` 中查阅配置方法。

**Q：部署后访问页面出现空白或样式错乱，如何排查？**

A：首先检查浏览器控制台是否有 JavaScript 报错，常见原因为静态资源路径在子目录部署时未正确配置 `basePath`。请在 `/config/default.json` 中调整 `publicPath` 字段，若使用 Nginx 反向代理，请确认代理转发规则正确处理了静态文件扩展名。运行 `npm run build` 生成静态版本后，检查输出目录中的 `index.html` 是否引用了正确的相对路径。

## 许可证

本项目的全部源代码及文档均采用 MIT 许可证进行授权。任何人可以免费获取、修改、分发本软件，包括用于商业目的，但需保留原始版权声明和许可声明。完整许可证文本请参见项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
