# NavHub

NavHub 是一个面向技术团队与内容运营者的外链资源聚合与导航系统。该项目定位于将分散在多处的优质技术文档、社区入口、数据分析工具以及行业资讯进行集中管理与快速呈现，帮助开发者在日常研发流程中减少信息查找成本，提升决策效率。NavHub 并非一个传统的内容生产平台，而是一个高度定制化的资源索引中间件，能够适配从个人知识库到团队协作门户的多种部署场景。

本项目特别适用于需要频繁访问外部技术参考、开源社区入口与各类数据查询工具的技术人员。当前版本已经集成了针对特定行业数据源的快速引用能力，并提供标准化的 URL 分发与展示规范，确保资源链接的稳定可溯。

## 功能概览

- **多源异构资源聚合**：支持将不同协议、不同域名、不同可信等级的链接统一收录，并进行分类标签化管理，便于团队内部共享。

- **快速检索与过滤**：内置轻量级前端检索机制，允许用户根据关键词、分类标签或收录批次快速定位目标链接，减少页面滚动查找时间。

- **链接状态健康检查**：后台定时任务自动探测已收录 URL 的可达性，并对失效链接进行标识和告警，保证资源库的长期可用性。

- **访问统计与热度排序**：记录每条外链的点击频次，支持按周、月维度生成访问趋势，帮助团队识别高频使用的核心资源。

- **标准化输出与导出**：支持将选定的资源列表以 Markdown 表格、纯文本或 JSON 格式导出，便于嵌入其他文档系统或自动化脚本。

- **多租户权限隔离**：针对企业级部署，提供基于角色的访问控制，不同项目组或部门可维护独立的资源视图，互不干扰。

- **操作日志审计**：所有增删改操作均记录详细操作日志，满足内部合规审计要求，便于追溯资源变更历史。

## 应用场景

- **技术团队知识库维护**：技术负责人可以使用 NavHub 集中管理团队常用的 API 文档入口、内部工具地址与云平台控制台链接，新成员入职时只需访问一个导航页面即可获取全部必要资源。

- **数据分析与决策支持**：数据科学团队可以将外部数据源、行业报告站、实时行情页面统一收录至 NavHub，并结合标签体系区分生产环境与测试环境的数据入口，避免误用。

- **开源社区资源整合**：开源项目维护者可将多个镜像站、依赖仓库地址、CI/CD 服务控制台以及社区讨论区入口聚合在一个页面中，方便贡献者快速切换上下文。

- **运营活动外链管理**：运营人员可将不同渠道的推广页面、活动报名表单、素材下载地址集中托管于 NavHub，配合访问统计功能评估各渠道的引流效果。

## 快速开始

以下步骤将帮助您在本地环境中快速启动 NavHub 服务。

```bash
# 1. 克隆项目仓库
git clone https://github.com/navhub/navhub.git

# 2. 进入项目目录并安装依赖
cd navhub
pip install -r requirements.txt

# 3. 初始化本地配置文件
cp .env.example .env

# 4. 初始化数据库表结构
python manage.py migrate

# 5. 启动开发服务器
python manage.py runserver --host 0.0.0.0 --port 8080
```

完成上述步骤后，访问 `http://localhost:8080` 即可看到 NavHub 的初始导航界面。默认管理员账号为 `admin`，密码为 `navhub2024`，首次登录后请及时修改。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 或更高 | 核心运行环境，建议使用 3.11 以获取更好的性能表现 |
| PostgreSQL | 13.0 或更高 | 主数据库，用于存储资源元数据、用户信息及操作日志 |
| Redis | 6.0 或更高 | 缓存与消息队列后端，用于提升检索性能及异步任务调度 |
| Node.js | 18.0 或更高 | 仅用于前端静态资源构建，生产环境无需运行时依赖 |
| Nginx | 1.20 或更高 | 推荐作为反向代理服务器，用于处理静态文件与负载均衡 |
| Git | 2.30 或更高 | 用于版本管理与热更新部署，非强依赖但强烈建议安装 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户指南 | `/docs/user-guide/` | 如何注册账号、创建分类、添加链接、使用检索与导出功能 |
| 管理员手册 | `/docs/admin-manual/` | 如何配置健康检查策略、管理用户权限、查看操作审计日志 |
| 部署运维 | `/docs/deployment/` | 如何在不同操作系统上部署生产环境，包括 Docker 编排与高可用配置 |
| 开发者文档 | `/docs/developer/` | 如何二次开发插件、扩展数据源适配器、定制前端主题 |
| API 参考 | `/docs/api/` | 对外提供的 RESTful API 接口定义、请求示例与错误码说明 |

## 资源列表

以下为本批次收录的全部外链资源，按类别分组展示。所有链接均已通过初步可达性验证。

**行业数据预测类**

- <code>zuqiubifentuijian.net.cn</code>
- <code>jinrizuqiutuijian.net.cn</code>
- <code>zuqiuyuce.net.cn</code>
- <code>zuqiuaiyuce.net.cn</code>

**赛事前瞻与推荐类**

- <code>zuqiuwendantuijian.org.cn</code>
- <code>zuqiusaiqiantuijian.org.cn</code>
- <code>zuqiubisaiqianzhan.org.cn</code>

**数据情报与资讯类**

- <code>zuqiuyucezixun.org.cn</code>
- <code>zuqiuyuceqingbao.org.cn</code>
- <code>zuqiufenxizixun.org.cn</code>

## 项目结构

```
navhub/
├── app/                                # 主应用核心代码目录
│   ├── controllers/                    # 控制器层，处理 HTTP 请求与响应
│   │   ├── link_controller.py          # 链接资源的增删改查逻辑
│   │   ├── category_controller.py      # 分类树管理接口
│   │   └── stats_controller.py         # 点击统计与热度计算
│   ├── models/                         # 数据模型定义，对应 PostgreSQL 表结构
│   │   ├── link.py                     # 链接实体模型，含 URL、标题、状态字段
│   │   ├── user.py                     # 用户与角色关联模型
│   │   └── audit_log.py                # 操作日志模型
│   ├── services/                       # 业务服务层，封装核心功能逻辑
│   │   ├── health_check.py             # 定时健康检查调度器
│   │   ├── cache_manager.py            # Redis 缓存策略管理
│   │   └── export_generator.py         # 多格式导出生成器
│   └── utils/                          # 工具函数集合
│       ├── validators.py               # URL 格式校验与规范化工具
│       └── markdown_render.py          # 资源列表转 Markdown 渲染器
├── frontend/                           # 前端静态资源源码目录
│   ├── assets/                         # 图片、字体等静态资源
│   ├── scripts/                        # 原生 JavaScript 交互逻辑
│   └── styles/                         # 自定义 CSS 样式及主题变量
├── tests/                              # 单元测试与集成测试用例
│   ├── unit/                           # 针对模型与服务的隔离测试
│   └── integration/                    # 端到端 API 测试
├── docs/                               # 全部文档的源文件存放位置
│   ├── user-guide/
│   ├── admin-manual/
│   └── developer/
├── scripts/                            # 运维辅助脚本
│   ├── init_db.sql                     # 数据库初始化脚本
│   └── cron_health.sh                  # 健康检查定时任务壳脚本
├── requirements.txt                    # Python 依赖清单
├── .env.example                        # 环境变量配置模板
└── README.md                           # 项目说明文档（当前文件）
```

## 贡献指南

我们欢迎各类形式的贡献，包括但不限于新增资源推荐、修复链接失效、改进文档表述以及提交功能增强补丁。请遵循以下流程：

1. 在 GitHub 仓库中 Fork 本项目的开发分支 `dev`，并克隆至本地环境。建议在个人分支上进行所有修改，避免直接操作主分支。

2. 若为新增资源链接，请确保提供完整的资源名称、所属分类以及至少一个使用场景说明。提交前请运行 `python manage.py validate_links` 命令验证所有链接的格式合法性。

3. 若为代码改动，需在 `tests/` 目录下补充对应的测试用例，并确保所有现有测试通过。提交信息请遵循语义化提交规范，以 `feat:`、`fix:`、`docs:` 作为前缀。

4. 提交 Pull Request 时，请清晰描述改动内容、动机以及影响范围。对于涉及数据库模型变动的 PR，必须附带迁移脚本。

5. 所有贡献者需在 PR 描述中同意项目许可证条款，并确保所提交内容不侵犯第三方版权或包含恶意外部链接。

## 常见问题

**问：健康检查任务发现链接失效后，系统会自动删除该链接吗？**

不会自动删除。系统仅将失效链接的状态标记为 `unreachable`，并在管理面板中高亮显示。管理员可以手动确认后决定保留、更新或移除。这是为了防止因目标站点临时维护导致误删除重要资源。

**问：如何批量导入外部链接列表，而不需要一条一条手动添加？**

NavHub 支持通过管理后台的 CSV 导入功能完成批量操作。您需要准备一个包含 `url`、`title`、`category` 三列的 CSV 文件，并在导入时选择目标分类。系统会自动校验 URL 格式并跳过重复条目。

**问：导出的 Markdown 资源列表能否直接用于其他文档系统？**

可以。导出的 Markdown 内容严格遵循通用 Markdown 语法，兼容 GitHub、GitLab、Gitea 以及多数知识库软件。您可以选择导出全部链接或按分类筛选导出，方便嵌入不同的技术文档或周报中。

## 许可证

本项目采用 MIT 许可证进行开源。您可以在遵守许可证条款的前提下自由使用、修改、分发本软件，包括用于商业目的。详细的许可证文本请参见项目根目录下的 LICENSE 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
