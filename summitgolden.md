# 资源导航站 - ResourceHub

ResourceHub 是一个面向技术开发者与内容研究者的高质量外链聚合平台，专注于收录互联网中具有长期价值的技术资源、文档站点与垂直领域信息源。项目本身不生产内容，而是通过人工筛选与自动化检测相结合的方式，维护一份结构清晰、可验证的外部资源索引，帮助用户快速定位特定领域的高质量信息入口。项目目标用户包括全栈工程师、技术写作人员、内容运营者以及信息安全方向的研究人员，解决的核心痛点是分散信息源的发现成本过高与链接时效性难以保障的问题。

## 功能概览

- **分类索引体系**：资源按地理区域、内容主题与文件类型进行多维度标签划分，支持快速筛选与批量导出。
- **链接健康监测**：每日定时检测收录链接的可达性与响应状态码，自动标记异常链接并生成报告。
- **元数据增强**：为每个资源条目补充服务器地理位置、SSL 证书信息、域名注册年限与历史快照对比。
- **全文检索支持**：基于标题、描述、标签与来源站点的关键词检索，响应时间低于 200 毫秒。
- **外部引用分析**：展示每个资源被其他技术社区或论文引用的频次与上下文片段。
- **版本变更追踪**：记录资源页面内容的关键变更（标题、描述、关键词），便于回溯历史状态。
- **批量导入导出**：支持 CSV 与 JSON 格式的资源列表批量导入，以及按条件筛选后的数据导出。
- **RSS 订阅生成**：为每个分类或标签生成独立的 RSS 订阅源，便于集成到阅读器中。

## 应用场景

- **技术文档写作与引用**：技术博主或文档撰写者需要引用特定地区的技术案例或数据源时，可通过本项目的分类索引快速找到合规且稳定的外部链接，避免使用被屏蔽或失效的引用地址。
- **网络资源归档研究**：研究人员对特定领域的对外信息传播模式进行分析时，可利用本项目的元数据增强与版本追踪功能，获取域名注册趋势、地理位置分布与内容变更历史，支撑定量研究。
- **内容运营与合规审核**：企业内容审核团队需要定期复核运营中引用的外部链接是否合规或是否仍处于活跃状态，可通过链接健康监测模块批量获取检测报告，减少人工逐一点击验证的时间成本。
- **个人知识库构建**：开发者或产品经理在构建个人知识管理系统时，可借助本项目的批量导出功能，将经过预筛选的资源列表直接导入 Notion、Obsidian 或 Roam Research 等工具，作为知识图谱的外部节点。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，确保系统已安装 Git、Node.js 18+ 与 npm 9+。

```bash
# 1. 克隆项目仓库
git clone https://github.com/resourcehub/resourcehub.git
cd resourcehub

# 2. 安装项目依赖
npm install

# 3. 复制环境变量模板并填写必要配置
cp .env.example .env
# 编辑 .env 文件，至少设置 DATABASE_URL 与 CHECK_INTERVAL

# 4. 初始化数据库结构
npm run db:migrate

# 5. 导入初始资源列表（包含本批次全部 10 个资源）
npm run import:initial

# 6. 启动开发服务器
npm run dev
```

访问本地 `http://localhost:3000` 查看资源导航面板。生产环境部署请参考 `docs/deployment.md`。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或更高 | 运行时环境，推荐使用 LTS 版本 |
| npm | 9.x 或更高 | 包管理器，用于依赖安装与脚本执行 |
| PostgreSQL | 14.x 或更高 | 主数据库，存储资源元数据与检测记录 |
| Redis | 7.x 或更高 | 缓存与任务队列后端，用于链接检测调度 |
| Git | 2.30 或更高 | 版本控制，用于克隆与提交变更 |
| 网络出口 | 开放 443/80 端口 | 链接健康检测需向外发起 HTTPS/HTTP 请求 |
| 系统时区 | UTC+8（可配置） | 检测时间戳默认使用系统时区，支持环境变量覆盖 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `docs/user-guide/` | 如何检索资源、解读元数据字段、订阅 RSS 以及导出列表？ |
| 管理员手册 | `docs/admin-guide/` | 如何配置检测频率、管理标签体系、处理异常链接与审核新增资源？ |
| 开发指南 | `docs/development/` | 项目架构分层、API 接口规范、新增检测器扩展方式与单元测试编写？ |
| 部署运维 | `docs/deployment/` | 支持 Docker Compose 与 K8s 两种部署方式，包含环境变量详解与监控告警配置？ |
| 数据格式 | `docs/data-format/` | 资源条目的完整 JSON Schema 定义、标签规范与历史版本记录格式？ |
| 贡献流程 | `docs/contributing/` | PR 提交规范、代码风格检查、Commit Message 模板与 Review 流程？ |

## 资源列表

### 地理区域 - 亚洲

<code>guochanyoudayouhuang.org.cn</code>

<code>wuyerenqi.org.cn</code>

<code>yazhouchengrenyiquerqu.org.cn</code>

<code>guochanrihanoumei.org.cn</code>

<code>rihanmadou.org.cn</code>

### 地理区域 - 欧洲与美洲

<code>oumeizhongchu.org.cn</code>

<code>oumeihouru.org.cn</code>

### 内容主题 - 综合

<code>tiantianyue.org.cn</code>

<code>yirenjiujiu.org.cn</code>

<code>sihujingpin.org.cn</code>

## 项目结构

```
resourcehub/
├── src/
│   ├── core/                     # 核心业务逻辑
│   │   ├── detector/             # 链接健康检测器实现
│   │   ├── indexer/              # 资源索引与检索服务
│   │   └── metadata/             # 元数据增强（地理位置、SSL、WHOIS）
│   ├── api/                      # RESTful API 路由层
│   │   ├── v1/                   # 当前稳定版 API
│   │   └── v2/                   # 实验性 API（开发中）
│   ├── scheduler/                # 定时任务调度器（基于 Redis Bull）
│   ├── web/                      # Web UI 面板（Next.js 应用）
│   │   ├── app/                  # 页面路由与布局
│   │   ├── components/           # 可复用 UI 组件
│   │   └── hooks/                # 自定义 React Hooks
│   ├── worker/                   # 后台工作进程（独立于主进程）
│   └── utils/                    # 通用工具函数（日志、加密、校验）
├── migrations/                   # 数据库迁移脚本（按时间戳命名）
├── seed/                         # 初始数据种子文件（包含本批次 URL）
├── tests/                        # 单元测试与集成测试
│   ├── unit/                     # 单测覆盖率目标 > 85%
│   └── integration/              # 需连接测试数据库运行
├── docs/                         # 完整文档（见上文文档导航）
├── scripts/                      # 运维辅助脚本（备份、恢复、迁移）
├── .github/                      # GitHub Actions CI/CD 流水线
│   └── workflows/
│       ├── test.yml              # 每次 PR 自动运行测试
│       └── deploy.yml            # 主分支合并后触发部署
├── docker-compose.yml            # 本地开发环境编排（PostgreSQL + Redis）
├── Dockerfile                    # 生产环境多阶段构建镜像
├── package.json                  # 项目依赖与脚本定义
└── README.md                     # 本文件
```

## 贡献指南

1. **Fork 仓库并创建功能分支**：从主仓库 Fork 到个人账户，然后基于 `main` 分支创建 `feature/your-feature-name` 或 `fix/issue-number` 分支，避免直接在 main 分支上修改。
2. **遵循代码规范与测试要求**：运行 `npm run lint` 与 `npm run format` 检查代码风格，确保新增或修改的代码有对应的单元测试，且测试覆盖率不低于当前基线。
3. **提交 Pull Request 前完成自检**：PR 描述中需清晰说明变更目的、影响范围与测试结果，关联相关 Issue 编号，并确保 CI 流水线（测试 + 构建）全部通过。
4. **资源列表更新需附带验证信息**：若新增或移除外部资源链接，需在 PR 中提供该链接的健康检测截图或检测日志，以及简要的收录理由或移除原因。
5. **文档同步更新**：任何影响用户行为或配置方式的变更，须同步更新 `docs/` 目录下的对应文档，并检查 README 中的快速开始与安装要求部分是否需要调整。

## 常见问题

**Q：项目内置的链接健康检测是否会频繁请求外部资源，导致被检测方封禁 IP？**

A：检测器默认采用指数退避策略，每个资源的最小检测间隔为 6 小时，且并发请求数限制为 5 个，同时随机化请求时间窗口（±15 分钟）。请求头中设置了合法的 User-Agent 与来源标识，并遵循 robots.txt 中的爬取延迟指令。对于返回 429 状态码的资源，系统会自动将其加入冷却名单，暂停检测 24 小时。

**Q：我能否将本项目部署在内网环境，仅用于内部资源管理？**

A：完全支持。项目所有外部依赖（PostgreSQL、Redis）均可替换为内网自建实例，且链接健康检测器支持配置代理服务器（HTTP_PROXY / HTTPS_PROXY），允许通过内网代理访问外网资源。若完全隔离内网，可关闭检测功能，仅保留索引与检索能力。

**Q：资源列表中的链接出现变更或失效时，项目会如何处理？**

A：检测器会根据 HTTP 状态码、页面标题变化与关键 meta 标签对比来判定资源状态。状态标记分为「正常」「重定向」「内容变更」「无法连接」「证书过期」五类。管理员可在 Web 面板中批量查看异常列表，并选择「更新元数据」「标记为失效」或「移除资源」。所有历史状态变更均记录在数据库的 `resource_history` 表中，支持按时间线回溯。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
