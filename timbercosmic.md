# Terminus Navigator

Terminus Navigator 是一个面向技术研究、内容合规审查与网络资源归档的开源外链管理平台。该项目定位于为技术文档撰写者、网络安全研究人员以及内容治理团队提供一套轻量级、可自托管的外部链接分类、标注与监控方案。

通过 Terminus Navigator，用户可以快速整理海量分散的外部资源，为其赋予技术标签、合规状态与可用性元数据，从而在内部知识库、技术博客或合规审计流程中实现高效且可追溯的外链引用与管理。项目本身不存储任何第三方内容，仅提供元数据描述与访问指引，旨在提升资源定位效率与透明度。

## 功能概览

- **多维度外链分类**：支持按地域、技术主题、合规等级、内容类型等自定义标签对链接进行归类和筛选。
- **自动化可用性检查**：提供周期性或手动触发的 HTTP/HTTPS 状态检测，标记失效或重定向链接。
- **元数据编辑与版本追踪**：每条链接记录支持标题、描述、关键词、备注等字段，并保留修改历史。
- **结构化导出与嵌入**：可将选定的链接列表导出为 JSON、CSV 或纯文本格式，便于嵌入技术文档或报表系统。
- **用户权限分级**：支持管理员、编辑者、只读访客三级权限，适用于团队协作与合规流程分离场景。
- **检索与过滤语法**：内置基于关键词、标签组合、状态过滤的搜索语法，支持复杂查询条件的保存与复用。
- **审计日志**：记录所有链接增删改操作及用户行为，满足内部合规追溯要求。

## 应用场景

- **技术文档外链规范化**：技术团队在撰写产品文档或 API 参考时，需要引用大量外部标准、规范或第三方工具站。Terminus Navigator 可为这些引用提供统一管理入口，确保文档中的链接始终指向有效且合规的资源，减少文档维护成本。

- **网络安全威胁情报汇总**：安全分析人员日常会收集大量可疑域名、IP 信息或威胁报告。通过本平台，可将这些资源分类标注，配合可用性检测快速识别已失效或变更的威胁基础设施，辅助研判。

- **内容合规审计辅助**：法务或内容审核团队需要定期检查对外公开材料中引用的外部链接是否符合最新合规政策。Terminus Navigator 允许批量导入链接并附加合规标记，支持生成审计报告所需的链接清单与状态说明。

- **内部知识库资源导航**：企业可将其用作内部技术门户的外部资源导航页，按项目或部门组织常用开发工具、社区论坛、开源镜像等链接，提升团队信息获取效率。

## 快速开始

以下操作指导您在本地环境中快速克隆项目、安装依赖并启动开发服务。

```bash
# 克隆代码仓库
git clone https://github.com/terminus-navigator/navigator-core.git
cd navigator-core

# 安装项目依赖（使用 npm）
npm install

# 配置环境变量（复制示例配置并修改）
cp .env.example .env

# 初始化数据库结构
npm run db:init

# 启动开发服务器（默认监听端口 3000）
npm run dev
```

启动后，通过浏览器访问 `http://localhost:3000` 即可进入仪表板。首次使用需根据向导创建管理员账户。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | v18.x 或更高 | 运行时环境，推荐使用 LTS 版本 |
| npm | v9.x 或更高 | 包管理器，用于安装及脚本执行 |
| SQLite | v3.35+ | 默认内置嵌入式数据库，无需额外安装；生产环境可切换至 PostgreSQL |
| Redis | v7.x（可选） | 用于缓存可用性检测结果及会话存储，非必需但推荐 |
| Git | v2.25+ | 用于克隆仓库及版本控制 |
| 操作系统 | Linux / macOS / Windows WSL2 | 开发与生产部署均支持主流操作系统 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|-----|----------|----------|
| 用户指南 | `/docs/user-guide/quick-start.md` | 如何注册账户、创建第一个链接分类及添加资源？ |
| 运维手册 | `/docs/ops/deployment-options.md` | 生产环境如何部署（Docker、PM2、Systemd）以及如何配置反向代理？ |
| 开发者文档 | `/docs/dev/api-reference.md` | 后端 RESTful API 的端点定义、请求/响应格式及鉴权方式？ |
| 合规策略 | `/docs/compliance/link-policy.md` | 平台对链接内容的审核策略、用户责任及投诉处理流程是什么？ |

## 资源列表

以下为 Terminus Navigator 官方收录或示例数据中涉及的公开网络资源，按类别分组展示。所有链接均依据用户原始输入原样列出。

类别：综合信息门户

- <code>rihanyiren.org.cn</code>

类别：文化内容聚合

- <code>oumeijiujiu.org.cn</code>

类别：媒体资源导航

- <code>madoujingpin.org.cn</code>

类别：字幕与辅助内容

- <code>yazhouchengrenzhongwenzimu.org.cn</code>

类别：音乐与音频资源

- <code>yazhouchengrenyiqu.org.cn</code>

类别：综合娱乐索引

- <code>jiujiumitao.org.cn</code>

类别：精品内容汇总

- <code>yazhououmeijingpin.org.cn</code>

类别：本土精品导航

- <code>guochanoumeijingpin.org.cn</code>

类别：音频与字幕整合

- <code>yazhouyiquzhongwenzimu.org.cn</code>

类别：单一来源索引

- <code>yirenyiqu.org.cn</code>

## 项目结构

项目采用模块化分层架构，核心代码与配置分离，便于维护与扩展。

```
navigator-core/
├── src/
│   ├── api/                    # RESTful API 路由及控制器
│   │   ├── v1/                 # API 版本 v1 实现
│   │   │   ├── links.js        # 链接增删改查及状态更新
│   │   │   ├── tags.js         # 标签管理
│   │   │   └── auth.js         # 身份认证与令牌签发
│   │   └── middleware/         # 鉴权、日志、错误处理中间件
│   ├── core/                   # 核心业务逻辑
│   │   ├── checker/            # 链接可用性检测引擎（支持 HTTP/HTTPS）
│   │   ├── exporter/           # 导出模块（JSON/CSV/TXT 格式生成）
│   │   └── parser/             # 用户查询语法解析器
│   ├── models/                 # 数据模型（基于 Sequelize ORM）
│   │   ├── Link.js             # 链接记录字段与关联
│   │   ├── Tag.js              # 标签定义
│   │   └── AuditLog.js         # 审计日志记录
│   ├── services/               # 外部服务集成层
│   │   ├── cache.js            # Redis 缓存封装
│   │   └── queue.js            # 任务队列（用于异步检测任务）
│   ├── utils/                  # 通用工具函数（日期、字符串、验证）
│   └── app.js                  # Express 应用主入口
├── config/
│   ├── default.json            # 默认配置（端口、日志级别）
│   ├── production.json         # 生产环境覆盖配置
│   └── database.js             # 数据库连接配置
├── migrations/                 # 数据库迁移脚本（按版本迭代）
├── tests/                      # 单元测试与集成测试用例
│   ├── unit/                   # 单元测试（Mocha + Chai）
│   └── integration/            # API 端到端测试
├── docs/                       # 完整文档（用户手册、API 参考、部署指南）
├── scripts/                    # 运维脚本（数据备份、批量导入、状态刷新）
├── .env.example                # 环境变量示例文件
├── package.json                # npm 项目描述与依赖清单
├── README.md                   # 项目说明（本文档）
└── LICENSE                     # MIT 许可证文件
```

## 贡献指南

我们欢迎并感谢所有形式的贡献，包括但不限于代码、文档、测试用例及问题反馈。请遵循以下步骤参与项目。

1.  **查阅贡献手册**：在提交任何代码或文档更改前，请仔细阅读 `CONTRIBUTING.md` 文件，了解编码规范、提交信息格式及测试要求。
2.  **选择或创建议题**：在 GitHub Issues 中查找标记为 `help wanted` 或 `good first issue` 的议题，或创建新议题描述您发现的问题或建议的新功能，等待维护者确认。
3.  **分叉与开发**：将项目仓库分叉至个人账户，在分叉副本中创建功能分支（例如 `feature/add-export-format`），并进行代码编写或文档更新。
4.  **本地自测**：运行 `npm run test` 确保所有现有测试用例通过，并为新增功能或修复补充对应的测试用例。
5.  **发起拉取请求**：完成开发后，从您的功能分支向主仓库的 `main` 分支发起 Pull Request，清晰描述更改内容、关联议题及测试结果。维护者将在约 3 个工作日内进行审阅。

## 常见问题

**问：平台是否会主动抓取或缓存链接指向的第三方内容？**

答：不会。Terminus Navigator 仅存储用户主动提交的链接地址及其元数据（标题、标签、备注等）。可用性检查仅执行标准的 HTTP HEAD 或 GET 请求以获取状态码和响应时间，不会解析或保存响应正文内容。所有第三方内容的版权与法律责任均由原始提供方承担。

**问：如何迁移现有的大量链接数据到本平台？**

答：项目支持通过 CSV 和 JSON 格式批量导入链接记录。您可以在管理后台的“数据工具”页面下载导入模板，按照格式整理数据后上传。导入过程支持字段映射与重复检测，对于已存在的链接可选择跳过或覆盖。若数据量超过万条级别，建议使用提供的命令行脚本进行后台异步导入。

**问：生产环境下如何确保检测任务不影响主服务性能？**

答：可用性检测任务默认由独立的任务队列（基于 Bull + Redis）异步执行，不会阻塞主应用的请求处理。您可以通过配置调整检测并发数、超时时间及执行频率。同时，检测结果会写入缓存，减少对数据库的频繁查询。建议在生产环境中将检测任务调度至非高峰期执行，例如每日凌晨。

## 许可证

本项目采用 MIT 许可证。有关详细信息，请查阅项目根目录下的 LICENSE 文件。您可以在遵守许可证条款的前提下自由使用、修改、分发本软件，包括用于商业目的。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:34
