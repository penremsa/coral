# LinkPilot 技术资源导航系统

LinkPilot 是一个面向开发人员、技术研究者与内容策展人的轻量级外链资源整理与导航平台。该项目定位于解决个人或团队在维护大量外部技术链接、社区网站与工具资源时面临的分类混乱、检索困难与版本不同步问题。LinkPilot 提供基于标签与场景的静态站点生成能力，支持将任意 URL 集合组织为结构清晰、可快速部署的技术资源门户。

目标用户包括开源项目维护者、技术文档编写者、教育培训机构以及日常需要管理大量外链的开发者。LinkPilot 通过约定优于配置的目录结构与自动化构建流程，降低外链资源维护成本，确保所有链接可审计、可归类、可追溯。项目本身不依赖数据库，全部数据以 Markdown 与 YAML Front Matter 形式存储，便于版本控制与协同编辑。

## 功能概览

- **多维度链接分类体系**：支持按技术领域、内容类型、目标受众、合规等级等多个维度对链接进行标记与筛选，每个链接可归属多个分类，满足复杂组织需求。

- **自动化静态站点生成**：基于 Node.js 与 EJS 模板引擎，将 Markdown 格式的链接数据编译为完整的 HTML 静态页面，无需后端服务即可运行，兼容任何主流 Web 服务器。

- **标签与全文检索**：内置轻量级客户端搜索索引，支持按标签、标题、描述关键词快速定位目标链接，搜索响应时间低于 200 毫秒，适用于千级链接规模。

- **链接可用性健康检查**：集成定时巡检功能，可配置周期对收录链接进行 HTTP 状态码探测，自动标记失效或响应异常的链接，并生成健康报告。

- **导入与导出标准格式**：支持批量导入 CSV 与 JSON 格式链接数据，亦可导出为通用的书签 HTML 格式，便于与其他浏览器工具或导航系统进行数据交换。

- **自定义主题与布局**：提供多套预设配色方案与布局模板，支持通过修改 CSS 变量快速定制导航页面外观，满足品牌化部署需求。

- **权限分级与审核流程**：内置基于角色的访问控制模型，支持管理员、编辑者、访客三级权限，编辑者提交的链接变更需经管理员审核方可上线。

- **访问统计与热度排序**：基于页面浏览量自动计算链接热度，支持按最热、最新、最常失效等多种排序方式展示链接列表，辅助资源优化决策。

## 应用场景

- **技术团队内部知识库导航**：研发团队可使用 LinkPilot 统一整理内部常用的 API 文档、设计规范、CI/CD 工具链入口、日志平台等内部系统链接，新成员入职时可快速访问全部核心资源，减少沟通成本。

- **开源社区资源聚合门户**：开源项目社区可利用本系统搭建周边生态导航页，集中展示相关教程、视频课程、第三方工具、插件市场、论坛讨论等外部资源，提升社区活跃度与内容可发现性。

- **教育培训机构课程资料索引**：培训机构或高校实验室可将每门课程的参考链接、在线实验环境入口、习题集来源、视频回放地址等按学期与课程模块组织为独立导航子站点，方便学生课后自学与复习。

- **个人技术写作素材库**：技术博主或自由撰稿人可使用 LinkPilot 整理日常阅读积累的文章、论文、代码仓库、统计数据来源等，在撰写技术报告或评测内容时快速检索引用来源，保证引用准确性。

- **合规性外链审计与监控**：对于需要符合特定合规要求（如内容分级、地区访问限制）的组织，可通过 LinkPilot 的自定义标签功能标记每一条链接的合规状态，定期导出审计报表，降低法律风险。

## 快速开始

以下步骤将帮助您在本地环境中快速启动 LinkPilot 服务。

```bash
# 步骤 1: 克隆项目仓库
git clone https://github.com/linkpilot/linkpilot-core.git
cd linkpilot-core

# 步骤 2: 安装项目依赖
npm install

# 步骤 3: 构建静态站点并启动开发服务器
npm run build
npm run serve
```

执行上述命令后，开发服务器默认监听 `http://localhost:8080`。首次构建将自动生成示例数据，包含若干预置分类与链接条目。您可通过编辑 `./data/links` 目录下的 Markdown 文件来添加或修改链接，保存后页面将自动重新构建。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与开发服务器 |
| npm | 9.x 或 10.x | 包管理器，用于安装项目依赖 |
| Git | 2.30 或更高 | 版本控制工具，用于克隆仓库与管理配置变更 |
| 磁盘空间 | 至少 200 MB | 存储源代码、依赖包及构建生成的静态文件 |
| 内存 | 至少 512 MB | 构建过程与开发服务器运行所需最低内存，大型站点建议 1 GB |
| 操作系统 | Linux / macOS / Windows | 跨平台支持，Windows 下建议使用 WSL2 或 Git Bash |
| Python | 3.8 或更高（可选） | 仅当启用高级链接分析插件时需要 |
| 浏览器 | 现代浏览器（Chrome / Firefox / Edge） | 用于访问生成的导航页面与后台管理界面 |
| 网络 | 出站 HTTPS 连通性 | 用于链接健康检查与外部资源图标加载 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | `/docs/user-guide/` | 如何添加链接、如何创建分类、如何自定义主题、如何导入导出数据 |
| 管理员手册 | `/docs/admin-handbook/` | 如何进行权限配置、如何设置健康检查周期、如何审核编辑提交 |
| 开发参考 | `/docs/developer-guide/` | 项目整体架构设计、核心数据模型、插件扩展接口与 API 文档 |
| 部署运维 | `/docs/deployment/` | 如何部署到 Nginx / Apache / CDN、如何配置 HTTPS、如何做增量构建 |
| 常见工作流 | `/docs/workflows/` | 如何批量迁移旧书签、如何与其他 CI 工具集成、如何做内容备份 |
| 版本发布 | `/docs/releases/` | 每个版本的更新日志、破坏性变更说明、升级迁移步骤 |

## 资源列表

以下为 LinkPilot 项目推荐收录或关联参考的第三方资源链接。所有链接均按内容类别分组，并严格保持用户提供的原始格式。

### 综合参考类

- <code>91shaofu.org.cn</code>
- <code>97renqi.org.cn</code>
- <code>jiujiulunli.org.cn</code>

### 主题专区类

- <code>zhongwenzimuzhifusiwa.org.cn</code>
- <code>zhongwenzimumeinv.org.cn</code>
- <code>meinvwangzhan.org.cn</code>
- <code>oumeirenqi.org.cn</code>

### 专题内容类

- <code>chengrenjuchang.org.cn</code>
- <code>chengrenwuyejuchang.org.cn</code>
- <code>siwazhongwenzimu.org.cn</code>

## 项目结构

```
linkpilot-core/
├── bin/                                 # 可执行命令行脚本入口
│   ├── linkpilot-cli.js                # 主 CLI 工具，提供 init/build/serve 命令
│   └── health-check.js                 # 独立运行的链接健康检查脚本
├── config/                              # 全局配置文件目录
│   ├── default.yaml                    # 默认配置项（端口、构建路径、缓存策略）
│   ├── taxonomy.yaml                   # 分类与标签体系定义
│   └── plugins.yaml                    # 第三方插件启用与参数配置
├── data/                                # 数据存储目录（所有用户数据）
│   ├── links/                          # 链接条目，每个 .md 文件代表一条链接
│   ├── categories/                     # 分类定义，YAML 格式
│   └── users/                          # 用户权限配置文件
├── docs/                                # 项目文档（详见上方文档导航）
│   ├── user-guide/
│   ├── admin-handbook/
│   ├── developer-guide/
│   ├── deployment/
│   ├── workflows/
│   └── releases/
├── src/                                 # 源代码主目录
│   ├── core/                           # 核心数据模型与解析器
│   │   ├── link-parser.js              # 解析 Markdown 链接条目
│   │   └── taxonomy-resolver.js        # 分类与标签关系处理
│   ├── generators/                     # 静态站点生成器
│   │   ├── html-generator.js           # 将数据渲染为 HTML 页面
│   │   └── rss-generator.js            # 生成 RSS 订阅源
│   ├── templates/                      # EJS 视图模板
│   │   ├── layouts/                    # 基础布局模板
│   │   ├── partials/                   # 可复用组件（导航栏、卡片、分页）
│   │   └── pages/                      # 独立页面模板（首页、分类页、详情页）
│   ├── assets/                         # 静态资源（CSS、JavaScript、图片）
│   │   ├── styles/                     # SCSS 样式源文件
│   │   ├── scripts/                    # 前端逻辑（搜索、排序、图表）
│   │   └── images/                     # 默认图标与占位图
│   ├── utils/                          # 通用工具函数
│   │   ├── http-client.js              # 封装 Axios 用于健康检查
│   │   └── markdown-renderer.js        # Markdown 渲染辅助
│   └── index.js                        # 程序主入口，初始化并启动服务
├── tests/                               # 单元测试与集成测试
│   ├── unit/                           # 针对核心模块的单元测试
│   └── fixtures/                       # 测试用固定数据集
├── .env.example                         # 环境变量示例文件
├── .gitignore                           # Git 忽略规则
├── package.json                         # npm 项目配置与依赖声明
├── README.md                            # 项目主文档（即本文档）
└── LICENSE                              # MIT 许可证文件
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于提交新链接收录、完善文档、报告缺陷或提交代码改进。请遵循以下步骤参与本项目建设。

1. **查阅现有议题与项目看板**：访问 GitHub Issues 与 Projects 页面，确认您想要处理的问题或提议的新功能尚未被他人认领。若为新提议，请先创建一个议题描述您的改进方向，等待核心维护者反馈后再着手实施。

2. **派生仓库并创建功能分支**：将主仓库派生至您的个人账户，然后克隆派生仓库至本地。基于 `main` 分支创建新的功能分支，分支命名建议采用 `feature/描述` 或 `fix/描述` 格式，例如 `feature/add-export-json`。

3. **编写或修改内容并确保本地构建通过**：若您贡献的是链接数据，请按照 `data/links/` 下的示例格式添加，并确保所有字段完整。若为代码变更，请运行 `npm run test` 确认所有已有测试通过，并为新增功能补充对应的单元测试。执行 `npm run build` 确保静态站点可正常生成。

4. **提交变更并签署开发者原产地证书**：提交信息请采用约定式提交规范，如 `feat: 增加按点击量排序功能`。所有提交必须包含 Signed-off-by 行，表明您同意开发者原产地证书。可通过 `git commit -s` 自动添加。

5. **发起合并请求并等待审阅**：推送分支至您的派生仓库，然后向主仓库的 `main` 分支发起合并请求。请在合并请求正文中详细描述变更内容、测试结果以及相关议题编号。核心维护者将在 3 个工作日内进行审阅，可能要求进一步修改或补充信息。

## 常见问题

**问：LinkPilot 能否管理超过一万条链接？性能是否会显著下降？**

答：LinkPilot 的设计目标之一是支持中小规模链接集合（建议不超过 5000 条）的流畅管理。当链接数量超过 3000 条时，首次构建时间可能增加至 30 秒以上，但运行时检索与页面访问不受影响，因为所有数据在构建时已预生成为静态文件。若需管理更大规模数据，建议分拆为多个子站点实例，或使用企业级后端方案。我们正在规划增量构建功能以进一步优化大型站点的构建效率。

**问：如何自定义导航页面的品牌标识与配色方案？**

答：所有样式变量集中定义在 `src/assets/styles/_variables.scss` 文件中。您可以通过修改 `$primary-color`、`$font-family` 等变量来调整整体视觉风格。Logo 与 Favicon 请替换 `src/assets/images/logo.svg` 与 `src/assets/images/favicon.ico` 文件。若需深度修改页面布局，请编辑 `src/templates/layouts/` 下的 EJS 模板文件，修改后重新运行 `npm run build` 即可生效。

**问：链接健康检查功能是否会误报或消耗过多网络资源？**

答：健康检查默认采用并发请求方式，并发数限制为 5，超时时间设为 10 秒，以避免对目标服务器造成压力。检查逻辑仅验证 HTTP 状态码是否属于 2xx 或 3xx 范围，对于需要登录或存在反爬机制的站点可能返回 403 或 429，此类情况会被标记为“需人工确认”而非直接判定失效。您可以在 `config/default.yaml` 中调整 `healthCheck.ignoreStatusCodes` 列表来忽略特定状态码，减少误报。

## 许可证

本项目采用 MIT 许可证。您被授权自由使用、复制、修改、合并、发布、分发、再授权及销售本软件的副本，但须在软件的所有副本或重要部分中包含上述版权声明与许可声明。本软件按“原样”提供，不作任何明示或暗示的保证，包括但不限于适销性、特定用途适用性及非侵权性保证。有关详情请参阅项目根目录下的 LICENSE 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:25
