# Rihan Resource Hub

Rihan Resource Hub 是一个面向中文互联网内容创作者、站点运营者与数字档案研究者的高质量外链资源聚合项目。该项目不生产原始内容，专注于对特定领域公开可用站点进行系统性梳理、分类归档与可达性维护，解决信息碎片化、链接失效与检索成本高的问题。目标用户包括独立站长、内容策展人、市场调研人员以及关注特定文化圈层的普通用户。

本项目通过人工筛选与自动化健康检查相结合的方式，维护一个高存活率、高相关度的外链集合。项目本身不存储或分发任何受版权保护的素材，所有列出的资源均指向第三方站点，用户访问时需遵守各目标站点的使用条款。项目以 Markdown 格式提供结构化索引，并辅以站点状态标注与分类标签，便于用户快速定位与批量导入。

## 功能概览

- **分类索引体系**：按语种、地区与内容主题对资源进行一级分类，支持多维度筛选。
- **自动链接健康检查**：每日定时检测收录链接的可达性与响应状态码，自动标记异常条目。
- **站点元数据标注**：为每个收录条目提供包括域名注册日期、ICP 备案信息（如适用）、内容语言、更新频率在内的辅助元数据。
- **批量导出工具**：提供命令行脚本，支持将选中的链接列表导出为 JSON、CSV 或纯文本格式，方便下游工具链使用。
- **变更日志追踪**：记录每次资源列表的新增、删除与 URL 变更操作，支持按时间回滚查看历史版本。
- **用户订阅机制**：允许用户通过邮件或 RSS 订阅特定分类的更新通知，实时获取新增资源推送。
- **本地缓存镜像**：为高频访问的链接提供静态快照缓存（仅含页面标题与元描述），提升检索速度并减少目标服务器压力。

## 应用场景

- **内容运营与选题策划**：内容团队可通过本项目的分类索引，快速发现特定语种或地区的最新热门站点，作为选题来源或竞品分析参考。例如，市场人员可定期查阅“日韩”分类下的更新，捕捉区域内容趋势。
- **站点迁移与友情链接重建**：当站长更换域名或重构站点时，可通过本项目的历史变更日志与健康检查记录，批量验证原有外链的有效性，并发现可替代的同类优质站点进行友链交换。
- **学术研究与文化观察**：研究人员可利用本项目的结构化元数据与导出功能，批量获取特定领域站点列表，用于网络内容分析、传播路径追踪或区域互联网生态比较研究。例如，可筛选“中文”与“境外”两个标签的交集，进行特定文化现象的跨地域观测。
- **个人导航页搭建**：普通用户可克隆本项目仓库，利用内置的导出脚本生成自定义的 HTML 导航页，部署在本地或私人服务器上，作为个人上网入口，避免依赖第三方书签服务的稳定性。
- **SEO 外链策略辅助**：SEO 从业者可依据项目的分类健康报告，筛选出高存活率、高更新频率的站点列表，作为外部链接建设的目标池，提升外链获取效率。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境。请确保已安装 Git 与 Node.js（版本 16.x 或以上）。

```bash
# 1. 克隆项目仓库
git clone https://github.com/rihan-resource-hub/public-index.git
cd public-index

# 2. 安装项目依赖（使用 npm）
npm install

# 3. 执行初始数据同步与健康检查
npm run sync
npm run health-check

# 4. 启动本地开发服务器（可选，用于预览 Web 界面）
npm run dev
```

执行完上述命令后，项目将在本地生成最新的资源索引文件（位于 `data/` 目录），并输出健康检查报告至 `reports/` 目录。用户可直接查看 `data/categorized-links.md` 文件获取分类列表，或通过 `http://localhost:3000` 访问本地 Web 预览界面。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 16.x 或更高 | 项目核心运行环境，用于执行脚本与构建工具链 |
| npm | 8.x 或更高 | Node.js 包管理器，用于安装项目依赖 |
| Git | 2.25 或更高 | 版本控制工具，用于克隆仓库与拉取更新 |
| 网络连接 | 稳定公网访问 | 用于进行链接健康检查及拉取远程资源元数据 |
| 系统内存 | 最低 512MB | 运行健康检查并发任务时的最低内存要求 |
| 磁盘空间 | 最低 200MB | 用于存储仓库副本、缓存数据与日志文件 |
| 可选：Docker | 20.10 或更高 | 如需使用容器化部署方式，需安装 Docker 及 Docker Compose |

## 文档导航

| 层面 | 目录/文件 | 回答的问题 |
|------|-----------|------------|
| 用户手册 | `docs/user-guide/classification.md` | 如何理解项目的分类体系与标签规则？如何按分类筛选资源？ |
| 用户手册 | `docs/user-guide/export-usage.md` | 如何使用导出脚本生成自定义格式的链接列表？参数选项有哪些？ |
| 运维手册 | `docs/operations/health-check.md` | 健康检查机制的原理是什么？如何调整检测频率与超时阈值？ |
| 运维手册 | `docs/operations/contribution-workflow.md` | 贡献者如何提交新增链接或更新现有条目？审核流程是什么？ |
| 开发手册 | `docs/development/api-reference.md` | 项目内部模块的 API 说明，如何扩展自定义分类器或导出格式？ |
| 开发手册 | `docs/development/local-debug.md` | 如何在本地完整搭建开发环境并进行端到端测试？常见调试技巧有哪些？ |

## 资源列表

本项目当前收录的核心外链资源按类别整理如下。所有链接均来自用户原始数据，未作任何修改。

**日韩地区分类**

- <code>rihanguochanyiqu.org.cn</code>
- <code>hanguorouputuan.org.cn</code>
- <code>yazhouribenguochan.org.cn</code>
- <code>yirenrihan.org.cn</code>

**中文人物与站点分类**

- <code>jingpinyiren.org.cn</code>
- <code>oumeizhongwenzimujingpinrenqi.org.cn</code>
- <code>tiantangyiren.org.cn</code>
- <code>zhongwenzimuyiren.org.cn</code>
- <code>zhongchushaofu.org.cn</code>

**其他内容分类**

- <code>tingtingyiquerqusanqu.org.cn</code>

## 项目结构

```bash
rihan-resource-hub/
├── .github/                         # GitHub 相关配置
│   └── workflows/                   # CI/CD 工作流（自动健康检查）
│       └── health-check.yml         # 每日定时执行链接检测
├── data/                            # 数据目录（核心资源索引）
│   ├── raw/                         # 原始输入数据（人工维护）
│   │   └── links-master.csv         # 主链接表（含分类与元数据）
│   ├── processed/                   # 处理后数据（脚本生成）
│   │   ├── categorized-links.md     # 按分类生成的 Markdown 索引
│   │   └── health-report.json       # 最新一次健康检查结果
│   └── cache/                       # 缓存目录（页面快照与元数据）
│       └── snapshots/               # 按域名存储的静态快照
├── docs/                            # 文档目录
│   ├── user-guide/                  # 用户手册
│   ├── operations/                  # 运维手册
│   └── development/                 # 开发手册
├── src/                             # 源代码目录
│   ├── core/                        # 核心模块
│   │   ├── classifier.js            # 分类器引擎
│   │   ├── health-checker.js        # 健康检查执行器
│   │   └── exporter.js              # 导出格式生成器
│   ├── cli/                         # 命令行入口
│   │   └── index.js                 # CLI 主程序
│   └── web/                         # Web 界面模块
│       ├── server.js                # 本地预览服务
│       └── templates/               # EJS 模板文件
├── tests/                           # 单元测试与集成测试
│   ├── unit/                        # 单元测试用例
│   └── integration/                 # 集成测试脚本
├── reports/                         # 运行报告输出目录
│   └── $(date)/                     # 按日期命名的报告子目录
│       └── health-summary.txt       # 文本格式健康摘要
├── scripts/                         # 辅助运维脚本
│   └── migrate-legacy.sh            # 旧数据迁移脚本
├── package.json                     # Node.js 项目配置文件
├── .env.example                     # 环境变量示例文件
└── README.md                        # 项目说明文档（本文件）
```

## 贡献指南

我们欢迎并感谢所有形式的贡献。请遵循以下步骤参与本项目：

1.  **查阅现有议题**：在提交新的贡献之前，请先浏览 GitHub Issues 列表，确认是否有类似的需求或问题已被提出。若无重叠，请创建一个新 Issue 描述您的建议（如新增链接、分类调整或功能请求）。
2.  **派生项目仓库**：点击项目页面右上角的“Fork”按钮，将本仓库派生至您的 GitHub 账号下。然后在本地克隆您派生后的仓库副本。
3.  **创建功能分支**：在本地仓库中，基于 `main` 分支创建一个新的分支。分支命名建议采用 `feature/add-domain` 或 `fix/update-category` 等清晰格式。
4.  **实施变更并自测**：
    -   若新增链接，请按照 `data/raw/links-master.csv` 中的格式规范填写所有必填字段（域名、分类、语言、备注）。
    -   若修改代码，请确保在 `tests/` 目录下补充或更新对应的单元测试，并执行 `npm test` 保证所有测试用例通过。
    -   执行 `npm run lint` 确保代码风格符合项目规范。
5.  **提交变更并推送**：编写清晰的 Commit Message，遵循 Conventional Commits 规范。将您的分支推送至您派生的远程仓库。
6.  **发起拉取请求**：在本项目页面点击“New Pull Request”，选择您的分支，详细描述变更内容、动机以及测试情况。项目维护者将在 2-3 个工作日内进行审核。

## 常见问题

**Q: 为什么部分链接在健康检查报告中显示为不可达？**

A: 健康检查结果受目标服务器可用性、网络环境、防火墙策略及临时维护等多重因素影响。项目每日仅执行一次检测，若某链接连续三次检测均失败，我们将在数据中将其标记为“待验证”状态，但不会立即删除，以留出站点恢复的时间窗口。用户可手动运行 `npm run health-check -- --force` 进行即时重新检测。

**Q: 如何请求新增某个特定站点到资源列表？**

A: 您可以通过 GitHub Issues 提交新增请求，或直接按照贡献指南中的步骤派生仓库并修改 `data/raw/links-master.csv` 文件后发起拉取请求。新增条目需满足以下基本要求：站点内容与现有分类主题相关、站点运营状态稳定（非短期临时页面）、且不违反项目的行为准则（禁止包含恶意软件、欺诈或极端违规内容）。

**Q: 该项目是否提供在线公开访问的 Web 服务？**

A: 目前项目以开源仓库形式为主，不提供官方托管的公开 Web 服务，以降低运营风险与维护成本。用户可自行通过 `npm run dev` 在本地启动预览界面，或使用导出脚本生成静态页面部署到个人服务器。我们鼓励有需求的用户基于本项目代码自行搭建私有化实例。

## 许可证

本项目代码与文档（不包括外部链接指向的第三方站点内容）采用 MIT 许可证开源发布。您可以在遵守许可证条款的前提下，自由使用、修改、分发本项目的代码与数据结构。有关第三方站点的内容版权问题，请直接联系对应的域名所有者。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:36
